# 《鸭压相册压缩工具》需求文档修改建议

## 1. 结论概览

当前这份 [副本鸭压-需求规格说明书.docx](/Users/borry5/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/wxid_aqwlk25k6lg222_3093/temp/drag/副本鸭压-需求规格说明书.docx) 整体框架是完整的，但里面混入了较多“早期设计方案”“iOS/Compose 风格描述”和“尚未实现的理想能力”。  
如果直接拿去和现在的 Android Java 重构工程对照，会出现以下问题：

- 有些功能已经改名，但文档还在用旧表述。
- 有些功能已经重构成新的交互，但文档还是旧流程。
- 有些能力文档写得过强，但当前工程实际上并没有实现。
- 有些关键实现已经完成，文档却没有及时更新。

因此建议把原文档从“设计目标说明”改成“当前版本真实需求说明 + 少量可扩展项说明”。

## 2. 主要不匹配点

### 2.1 技术栈描述不匹配

原文档问题：

- 文档参考资料中写了 `Android Jetpack Compose`、`Coil`。
- 缩略图和列表部分写了 `LazyVerticalGrid`。

当前工程实际情况：

- 当前项目是 `Android Java + XML`，不是 Compose。
- 浏览页网格使用 `RecyclerView + GridLayoutManager`。
- 缩略图加载使用自定义 [ThumbnailLoader.java](/Users/borry5/Android_homework/code/app/src/main/java/com/duckya/yaya/util/ThumbnailLoader.java)，不是 Coil。

修改建议：

- 删除 Compose、LazyVerticalGrid、Coil 相关表述。
- 改成 Java、XML、RecyclerView、Material Components、Media3 Transformer。

### 2.2 浏览本地模块功能写得过多

原文档问题：

- 写了“扫描进度卡片包含停止、暂停/继续、扫描速度、预计剩余”。
- 写了“支持本地文件、Live Photo/实况图筛选”。
- 写了“支持隐藏压缩前文件或压缩输出文件”。
- 写了“长按菜单包括分享”。
- 写了“分页加载更多媒体项”。

当前工程实际情况：

- 扫描页只支持开始扫描、重新扫描、显示已读取数量和进度，不支持扫描暂停、扫描停止、扫描速度、扫描剩余时间。
- 筛选只支持 `全部 / 照片 / 视频`。
- “不显示压缩出来的项目”功能已经按你的要求删除。
- 长按菜单只有“添加到压缩队列”“添加到回收队列”，没有分享。
- 当前没有真正的分页加载逻辑，而是一次性扫描后在 RecyclerView 中展示。

修改建议：

- 将扫描功能描述改为“开始扫描 / 重新扫描 / 进度展示 / 扫描结果缓存”。
- 将筛选描述改为“全部、照片、视频”。
- 删除 Live Photo、本地文件、隐藏压缩文件、分享、分页加载等描述。

### 2.3 扫描与缓存实现描述不准确

原文档问题：

- 文档写了“缩略图保存到 app cache 目录”“缓存索引扫描”“停止滑动后预取”“全局单任务限流”。

当前工程实际情况：

- 当前真正持久化的是扫描结果缓存：[MediaScanCache.java](/Users/borry5/Android_homework/code/app/src/main/java/com/duckya/yaya/util/MediaScanCache.java)，保存在 `filesDir/media_scan_cache.json`。
- 缩略图主要通过 [ThumbnailLoader.java](/Users/borry5/Android_homework/code/app/src/main/java/com/duckya/yaya/util/ThumbnailLoader.java) 动态加载：
  - Android 10+ 优先调用 `ContentResolver.loadThumbnail`
  - 低版本图片走采样解码
  - 低版本视频走 `MediaMetadataRetriever`
- 当前没有单独的磁盘缩略图索引系统，也没有“停止滑动后预取”的完整调度逻辑。

修改建议：

- 把“缩略图缓存与性能模块”改成“扫描结果缓存与缩略图异步加载优化”。
- 删除磁盘缩略图索引、分页预取、滑动停止后延迟预加载等尚未实现描述。

### 2.4 任务队列文案与流程部分过时

原文档问题：

- 还写着“左边已节省空间”。
- 删除功能大量使用“删除”表述。
- 任务队列入口回到已完成任务的旧交互没有更新。

当前工程实际情况：

- 已改成“累计节省空间”。
- 当前统一用“回收”表述，更符合系统回收站授权机制。
- 点击底部“任务队列”总是回到主队列页，而不是停留在已完成任务页。

修改建议：

- 把“删除队列”“删除任务”大部分改成“回收队列”“回收任务”。
- 保留“系统回收站授权”这一核心含义，但界面用词统一成“回收”。
- 明确任务队列有三个页面态：
  - 主队列页
  - 已完成任务页
  - 完成任务预览页

### 2.5 任务开始流程文档未更新

原文档问题：

- 文档把压缩任务和删除任务分开描述，但没有写“开始前先检查是否有待授权回收任务”。

当前工程实际情况：

- 当前点击开始时，如果存在待回收任务，会先发起系统回收授权，再继续后续压缩任务。
- 这部分逻辑在 [QueueFragment.java](/Users/borry5/Android_homework/code/app/src/main/java/com/duckya/yaya/ui/QueueFragment.java) 的 `startQueueWithRecycleCheck(...)` 和 [QueueManager.java](/Users/borry5/Android_homework/code/app/src/main/java/com/duckya/yaya/queue/QueueManager.java) 的相关流程里。

修改建议：

- 在执行队列用例里补充“开始前优先处理待授权回收任务”的说明。

### 2.6 预计剩余时间逻辑文档未更新

原文档问题：

- 原文只写“显示预计剩余时间”，没有写计算依据。

当前工程实际情况：

- 当前是按“已处理压缩任务的累计输入体积 / 已用时长”估算处理速度。
- 再结合剩余压缩任务的总输入体积，计算预计剩余时间。
- 逻辑在 [QueueManager.java](/Users/borry5/Android_homework/code/app/src/main/java/com/duckya/yaya/queue/QueueManager.java) 的 `estimatedRemainingTimeMs()`。

修改建议：

- 把预计剩余时间改写为“基于已完成和当前进行中的压缩任务体积速度估算”。

### 2.7 压缩参数设置模块与现状差异最大

原文档问题：

- 写的是“无感 / 平衡 / 强力”。
- 写了“编码格式 H.265、H.264 可选”。
- 写了“输出容器：继承原格式、MOV、MP4”。
- 写了“手动填写分辨率、手动帧率”。
- 写了“是否保存压缩对照相册”。
- 写了“音频码率/静音可调”。

当前工程实际情况：

- 图片压缩档位是 `视觉无损 / 朋友圈画质`，并不是“无感 / 平衡 / 强力”。
- 视频压缩档位是 `视觉无损 / 均衡压缩 / 分享画质 / 自定义`。
- 视频编码对用户不显示选择项，内部固定优先 `H.265`，失败自动回退 `H.264`。
- 音频不提供调节，始终保留原音频。
- 没有输出容器选择。
- 没有手动输入分辨率/帧率，只能从固定选项中选。
- “压缩对照”不是可选开关，而是默认始终保存原始版本和压缩版本。
- 当前分辨率选项是 `原始 / 4K / 2K / 1080P / 720P / 480P`。
- 当前帧率选项是 `原始 / 60 / 30 / 24`。
- `1GB` 限制只有原视频大于 `1GB` 时才允许使用。
- 只有选择 `自定义` 时，才展开高级设置区域。

修改建议：

- 彻底重写压缩参数设置章节，不要继续沿用旧版设计稿描述。

### 2.8 对照相册功能文档描述需要改成“固定能力”

原文档问题：

- 写成了“是否把压缩前后文件保存到对照相册”。

当前工程实际情况：

- 图片和视频压缩完成后，原始版本副本与压缩版本都会保存到系统相册 `压缩对照`。
- 这是当前固定能力，不是用户开关。

修改建议：

- 统一改为“系统自动保存原始版本和压缩版本到‘压缩对照’相册”。

### 2.9 数据存储说明已经落后

原文档问题：

- 文档说“任务状态当前主要保存在 App 运行时状态中，后续可扩展为本地持久化文件或数据库”。

当前工程实际情况：

- 任务状态已经通过 [QueueTaskStore.java](/Users/borry5/Android_homework/code/app/src/main/java/com/duckya/yaya/queue/QueueTaskStore.java) 持久化到应用私有目录的 JSON 文件。
- 扫描结果也已经通过 [MediaScanCache.java](/Users/borry5/Android_homework/code/app/src/main/java/com/duckya/yaya/util/MediaScanCache.java) 做了本地缓存。
- 当前仍然没有 SQLite / Room 数据库。

修改建议：

- 把数据存储说明改为：
  - 系统媒体依赖 MediaStore
  - 扫描结果缓存使用 JSON 文件
  - 队列任务状态使用 JSON 文件
  - 当前未引入数据库

### 2.10 用例文档中应删除未实现支线

建议删除或弱化的用例内容：

- 扫描暂停
- 扫描停止
- 分享媒体
- 隐藏压缩前/压缩后文件
- Live Photo / 实况图筛选
- 手动输入分辨率、帧率
- 输出容器选择
- 音频码率调节、静音压缩
- 对照相册开关
- 缩略图磁盘索引和预取泳道图中的复杂调度机制

## 3. 文档中应补充的真实实现点

建议补充这些当前已经实现、但原文没有准确写出的点：

- 顶部固定栏为“相册总览 + 选择”，进入多选后固定栏切换为“压缩 / 回收 / 完成”。
- 点击底部“任务队列”会直接回到主队列页。
- 主队列“清空”和已完成列表“清空列表”都带确认弹窗。
- 回收原文件或压缩文件时，如果另一份已经回收，会弹二次确认。
- 已完成任务支持进入独立二级页面查看。
- 图片完成任务支持原图/压缩图同步缩放对比。
- 视频完成任务支持双视频预览和回收。
- 视频设置修改会即时保存并刷新预估。
- 预计剩余时间是按压缩速度动态估算，而不是静态经验值。

## 4. 推荐修订策略

建议直接采用“整体重写正文”的方式，而不是在旧文档上零散修补。原因是：

- 原文中旧设计内容较多，零散修改容易留下前后矛盾。
- 你们当前工程已经成型，更适合写成“基于当前实现的真实需求文档”。
- 后续实验报告、答辩稿、演示流程都能直接和修订版保持一致。

## 5. 最终建议

最终建议是保留原文档的章节结构，但正文内容按当前 Android Java 重构版本重写。  
我已经另外生成了一份完整修订版文档，可直接作为新的需求规格说明书基础稿继续使用。
