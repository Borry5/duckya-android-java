# Java 版“鸭压”重构实施计划

> 状态图例：已完成✅ / 工作中🔵 / 等待校验🟡 / 未完成❌

## 0. 当前状态总览

| 阶段 | 状态 | 目标 |
| --- | --- | --- |
| 阶段 0：计划文档 | 已完成✅ | 明确 Java 版重构路线、阶段划分和测试标准 |
| 阶段 1：基础工程与主框架 | 已完成✅ | 搭好 Java + XML 的主界面框架 |
| 阶段 2：相册权限与媒体扫描 | 已完成✅ | 读取本地图片和视频，并展示扫描结果 |
| 阶段 3：媒体网格与预览 | 已完成✅ | 展示缩略图、媒体信息和预览弹窗 |
| 阶段 3.1：界面优化 | 已完成✅ | 优化相册页进度卡、筛选、排序和媒体卡片展示 |
| 阶段 3.2：滚动体验优化 | 已完成✅ | 相册页顶部进度、筛选和排序随内容滚出屏幕 |
| 阶段 3.2.2：扫描 ANR 修复 | 已完成✅ | 修复扫描后 0% 卡死和未响应问题 |
| 阶段 3.3：时间与码率排序维护 | 等待校验🟡 | 合并时间排序入口，并在卡片左上角显示日期或视频码率 |
| 阶段 4：任务队列基础能力 | 已完成✅ | 支持添加、展示、取消、清空任务 |
| 阶段 4.1：缓存与多选修复 | 已完成✅ | 缓存扫描结果，并支持相册多选批量加入队列 |
| 阶段 4.2：选择与扫描进度优化 | 已完成✅ | 优化选择模式刷新、按钮同步和扫描实时进度 |
| 阶段 4.3：选择交互性能修复 | 已完成✅ | 修复选择按钮、复选框反馈和原生控件位置体验 |
| 阶段 4.4：移除卡片复选框 | 已完成✅ | 删除下方卡片复选框，保留顶部选择和批处理按钮 |
| 阶段 5：图片压缩 | 已完成✅ | 完成图片压缩并保存到系统相册 |
| 阶段 5.1：切页卡顿修复 | 已完成✅ | 复用主页面并缓存扫描结果，修复从队列返回相册卡顿 |
| 阶段 5.2：压缩对照相册 | 已完成✅ | 把原图副本和压缩结果保存到系统相册“压缩对照” |
| 阶段 5.3：图标与黄色主题 | 已完成✅ | 更换 app 启动图标，并把主题主色改为黄色 |
| 阶段 5.4：对照文件名修复 | 已完成✅ | 修复多张图片压缩后原始版本和压缩版本名称错配问题 |
| 阶段 5.5：压缩图 EXIF 保留 | 已完成✅ | 压缩后保留 ISO、焦距、拍摄设备等照片元数据 |
| 阶段 5.6：照片压缩方案优化 | 已完成✅ | 重做视觉无损和朋友圈画质两种压缩策略 |
| 阶段 5.7：队列进度刷新防闪 | 已完成✅ | 修复压缩进度更新时任务缩略图跳动闪动 |
| 阶段 5.8：任务队列完成态与回收重构 | 已完成✅ | 拆分完成任务列表、回收入口和队列页吸顶布局 |
| 阶段 5.8.0：任务拆分与验收计划 | 已完成✅ | 明确 5.8 多子阶段执行顺序和测试标准 |
| 阶段 5.8.1：队列页文案与吸顶布局 | 已完成✅ | 固定标题、清空和开始按钮，去掉阶段说明和待处理卡片 |
| 阶段 5.8.2：已完成任务入口与列表 | 已完成✅ | 增加已完成任务入口，并单独展示所有完成任务 |
| 阶段 5.8.3：完成任务卡片与底部动作 | 已完成✅ | 完成任务展示已节省文案和回收/重新压缩按钮 |
| 阶段 5.8.3.1：隐藏空完成入口 | 已完成✅ | 没有已完成任务时不显示“已完成的任务”入口 |
| 阶段 5.8.3.2：已完成任务二级页面 | 已完成✅ | 已完成任务入口进入完整二级页面并支持返回 |
| 阶段 5.8.3.3：完成页排版与多选吸顶修复 | 已完成✅ | 修复完成页标题/返回按钮显示，并固定浏览页多选操作栏 |
| 阶段 5.8.3.4：完成卡片按钮整行铺满 | 已完成✅ | 已完成任务三个按钮占满整张卡片宽度并完整显示文字 |
| 阶段 5.8.4：系统相册回收接口 | 已完成✅ | 使用 Android MediaStore 删除授权框回收原始媒体 |
| 阶段 5.8.5：一键回收与清空完成列表 | 已完成✅ | 支持批量回收原始文件和清空已完成任务记录 |
| 阶段 5.8.6：联调验收与故障修复 | 已完成✅ | 验证完成态、回收授权、重新压缩和列表刷新 |
| 阶段 5.8.7：已完成任务预览与参数详情 | 已完成✅ | 点击完成任务缩略图查看大图/视频预览，并展示详细参数信息 |
| 阶段 5.8.8：完成任务图像对比页维护 | 已完成✅ | 预览改为完整二级页面，支持原图和压缩图同时手动放大对比 |
| 阶段 5.8.9：对比页快捷操作维护 | 已完成✅ | 在图像对比页补齐回收原图、回收压缩图和重新压缩入口 |
| 阶段 5.8.10：对比页提示栏节省空间维护 | 已完成✅ | 将原图/压缩图标签和三角方向提示合并为一行 |
| 阶段 5.8.11：完成列表清空确认维护 | 已完成✅ | 清空已完成任务列表前弹出确认对话框 |
| 阶段 5.8.12：压缩档位弹窗视觉规范维护 | 已完成✅ | 重做压缩档位弹窗的对比度、深浅色适配和 Material 视觉层次 |
| 阶段 5.8.13：压缩档位弹窗安全区与排版维护 | 已完成✅ | 重新整理弹窗的内容安全边距、标题层级和底部操作区 |
| 阶段 5.8.14：完成任务双版本回收确认 | 已完成✅ | 原图或压缩图已回收后，再回收另一份时进行二次确认 |
| 阶段 5.8.15：任务卡片按钮与体积排版维护 | 已完成✅ | 移除优先按钮，并重排任务卡片中的预估体积展示 |
| 阶段 5.8.16：取消按钮截断修复 | 已完成✅ | 提高取消按钮高度与内边距，修复文字被裁切问题 |
| 阶段 5.8.17：完成任务按钮状态与文案维护 | 已完成✅ | 原图回收后禁用重新压缩，并细化回收完成文案 |
| 阶段 5.8.18：图像对比窗口与同步缩放维护 | 已完成✅ | 缩小上下预览窗口高度，并实现原图/压缩图同步缩放与拖动 |
| 阶段 5.8.18.1：图像对比预览空白修复 | 已完成✅ | 修复同步缩放接入后预览区域只显示背景、不显示图片的问题 |
| 阶段 5.8.18.2：原图对比来源稳定性修复 | 已完成✅ | 对比页优先读取“压缩对照”里的原始版本副本，减少原始相册 URI 失效导致的空白 |
| 阶段 5.8.18.3：老任务原图反查与预览故障提示 | 已完成✅ | 老任务通过压缩图文件名反查原始版本副本，并在图片无法读取时显示明确提示 |
| 阶段 5.8.18.4：图像对比大图采样预览修复 | 已完成✅ | 预览页后台采样解码超大原图，避免 Canvas 拒绝绘制过大 bitmap |
| 阶段 6：删除到系统回收站 | 已完成✅ | 调用系统回收站授权并完成删除任务 |
| 阶段 7：集成验收与修正 | 未完成❌ | 跑通核心流程并修复问题 |
| 阶段 8：第二阶段视频压缩 | 等待校验🟡 | 接入 Media3 Transformer 做视频压缩 |
| 阶段 8.0：视频压缩方案与接口设计 | 已完成✅ | 明确视频档位、控件、数据模型和队列接入边界 |
| 阶段 8.1：Media3 依赖与基础转码验证 | 等待校验🟡 | 接入 Transformer 并跑通单个视频转码最小链路 |
| 阶段 8.2：视频压缩设置弹窗 | 等待校验🟡 | 为视频任务提供分辨率、编码、码率和音频保留选项 |
| 阶段 8.3：VideoCompressionWorker | 等待校验🟡 | 实现后台视频转码、进度回调、失败回退和取消能力 |
| 阶段 8.4：队列混合处理接入 | 等待校验🟡 | 图片和视频任务共用队列，按顺序处理并刷新进度 |
| 阶段 8.5：视频输出与压缩对照相册 | 等待校验🟡 | 原视频副本和压缩视频保存到系统相册“压缩对照” |
| 阶段 8.6：完成任务视频预览与回收 | 等待校验🟡 | 完成页展示视频参数、播放预览和回收原/压缩视频 |
| 阶段 8.7：兼容性、性能与验收修复 | 等待校验🟡 | 覆盖编码失败、长视频、后台切换和低端设备降级 |
| 阶段 8.8：视频设置弹窗与音频策略维护 | 等待校验🟡 | 默认 H.265，移除音频调节，优化视频设置弹窗排版 |
| 阶段 8.9：视频帧率与 1GB 码率限制维护 | 等待校验🟡 | 输出规格展示分辨率/帧率，按三档策略设置码率，并增加 1GB 限制 |
| 阶段 8.10：视频设置即时保存与预估刷新修复 | 等待校验🟡 | 固定 H.265 自动回退，修复画质档位保存和预估刷新 |
| 阶段 8.11：1GB 限制条件生效维护 | 等待校验🟡 | 只有原视频超过 1GB 时才允许使用 1GB 文件大小限制 |
| 阶段 8.12：视频档位与自定义展开维护 | 等待校验🟡 | 增加自定义档位，并补齐 4K/2K 与 60/30/24 帧率选项 |

## 1. 项目目标

Java 版第一版目标是做出一个核心可运行的 Android 相册清理压缩工具：

- 使用 Java + XML，不使用 Compose。
- 支持扫描本地图片和视频。
- 支持图片/视频预览。
- 支持把图片加入压缩队列。
- 支持把图片或视频加入删除队列。
- 支持图片压缩并保存到系统相册。
- 支持 Android 11+ 系统回收站授权删除。
- 视频压缩放到第二阶段实现。

第一版不追求完整复刻 Kotlin 参考代码的所有高级能力，优先保证流程清楚、代码稳定、容易演示。

## 2. 阶段拆分与验收标准

### 阶段 1：基础工程与主框架

状态：已完成✅

目标：

- 在 Android Studio 中创建 Java 基础项目。
- 使用 XML 布局搭建主界面。
- 用底部导航切换两个页面：`浏览本地`、`任务队列`。

主要实现：

- `MainActivity`
- `ScanFragment`
- `QueueFragment`
- `activity_main.xml`
- 底部 `BottomNavigationView`
- Fragment 容器

测试标准：

- App 可以正常启动。
- 底部有两个 Tab。
- 点击 `浏览本地` 和 `任务队列` 可以正常切换页面。
- 横竖屏切换时不崩溃。
- Logcat 无明显启动异常。

### 阶段 2：相册权限与媒体扫描

状态：已完成✅

目标：

- 申请相册读取权限。
- 通过 `MediaStore` 扫描系统相册里的图片和视频。

主要实现：

- Android 13 及以上申请：
  - `READ_MEDIA_IMAGES`
  - `READ_MEDIA_VIDEO`
- Android 12 及以下申请：
  - `READ_EXTERNAL_STORAGE`
- 图片扫描使用 `MediaStore.Images.Media`。
- 视频扫描使用 `MediaStore.Video.Media`。
- 扫描字段包括：
  - 文件名
  - Uri
  - 文件大小
  - 修改时间
  - 宽高
  - 视频时长

测试标准：

- 首次打开页面时，未授权状态能显示授权按钮。
- 点击授权按钮后能弹出系统权限申请。
- 用户授权后能扫描出本机图片和视频。
- 用户拒绝权限后页面不崩溃，并能继续提示授权。
- 没有媒体文件时显示空状态。

### 阶段 3：媒体网格与预览

状态：已完成✅

目标：

- 用三列网格展示扫描结果。
- 支持筛选、排序和媒体预览。

主要实现：

- `RecyclerView` + `GridLayoutManager`
- `MediaGridAdapter`
- 媒体卡片展示：
  - 缩略图
  - 文件大小
  - 图片 MP 信息或视频时长
- 筛选：
  - 全部
  - 图片
  - 视频
- 排序：
  - 按体积
  - 按时间
- `PreviewBottomSheet`
  - 展示大图或视频首帧
  - 展示名称、类型、大小、分辨率、时长、码率
  - 提供压缩和删除入口

测试标准：

- 图片和视频能在网格中显示。
- 图片缩略图能正常加载。
- 视频至少能显示首帧或视频占位图。
- 点击媒体能打开预览弹窗。
- 预览弹窗信息和原文件基本一致。
- 筛选全部/图片/视频结果正确。
- 按体积和按时间排序结果正确。

### 阶段 3.1：界面优化

状态：已完成✅

目标：

- 扫描完成后只展示 100% 进度条和重新扫描按钮，不再展示图片/视频分别数量。
- 新增独立筛选按钮和排序按钮，样式接近参考图但使用 Android 原生/Material 支持控件。
- 筛选中的资源类型功能真实生效。
- 默认按体积从大到小排序。
- 媒体缩略图卡片不显示文件名。

测试标准：

- 扫描完成后进度显示为 100%，按钮为重新扫描。
- 筛选按钮能打开筛选面板，全部/图片/视频切换后网格正确变化。
- 排序按钮能打开排序菜单，默认是按体积从大到小。
- 媒体卡片只显示缩略图、时长或 MP、文件大小，不显示文件名。
- 点击媒体仍能打开预览弹窗。

### 阶段 3.2：滚动体验优化

状态：已完成✅

目标：

- 相册页的标题、读取进度、筛选按钮、排序按钮和提示文案都作为内容参与滚动。
- 用户向下滑动后，顶部控制区会滑出屏幕，只剩媒体预览网格。

测试标准：

- 在媒体网格上向上滑动时，读取进度卡、筛选和排序栏会一起向上消失。
- 滚动过程中底部导航保持可用。
- 筛选、排序和预览功能仍正常。

### 阶段 3.2.2：扫描 ANR 修复

状态：已完成✅

目标：

- 修复授权后开始扫描时进度停在 0%，稍后应用未响应的问题。
- 保留 3.2 的滚动体验：顶部进度卡、筛选、排序可随内容滑出屏幕。

修复策略：

- 禁止把大量媒体项放进 `NestedScrollView` 中一次性测量。
- 改为单个 `RecyclerView` 承载 Header 和网格媒体项，Header 占满 3 列，媒体项继续按 3 列虚拟化加载。

测试标准：

- 授权后扫描不再卡死或弹出未响应。
- 扫描完成后进度变为 100%。
- 大量媒体时仍能滚动浏览。

### 阶段 3.3：时间与码率排序维护

状态：已完成✅

目标：

- 移除重复的“按拍摄时间”入口，只保留一个时间排序选项。
- 将排序文案统一为“按时间”。
- 在媒体卡片左上角显示与当前排序更相关的辅助信息。

主要实现：

- 排序菜单删除“按拍摄时间”，保留“按时间”。
- 当前按时间排序时，媒体卡片左上角显示文件日期，格式只显示月和日。
- 当前按视频码率排序时，媒体卡片左上角显示视频码率。
- 当前按视频码率排序时，列表自动过滤为仅显示视频内容。
- 多选模式下隐藏左上角角标，避免与复选框重叠。

测试标准：

- 排序菜单中不再出现“按拍摄时间”或“按添加时间”，只显示“按时间”。
- 选择“按时间”后，媒体卡片左上角显示月/日格式日期。
- 选择“按视频码率”后，列表自动切换为只显示视频。
- 选择“按视频码率”后，视频卡片左上角显示对应码率。
- 进入多选模式后，左上角日期/码率角标隐藏，复选框显示正常。
- 顶部 Header 仍能随媒体列表一起滑出屏幕。

### 阶段 4：任务队列基础能力

状态：已完成✅

目标：

- 建立任务队列数据层。
- 支持从扫描页添加压缩或删除任务。
- 队列页能展示任务列表和基础操作。

主要实现：

- `QueueManager` 单例
- `QueueTask`
- `MediaItemInfo`
- `CompressionSettings`
- 枚举：
  - `MediaKind`
  - `QueueAction`
  - `QueueStatus`
  - `CompressionPreset`
- 队列能力：
  - 添加单个任务
  - 批量添加任务
  - 取消任务
  - 清空队列
  - 重试失败任务
  - 优先处理任务
- UI 能显示：
  - 任务名称
  - 操作类型
  - 状态
  - 原始大小
  - 预计节省空间
  - 进度条

测试标准：

- 从预览弹窗点击压缩后，图片能加入压缩队列。
- 从预览弹窗点击删除后，图片或视频能加入删除队列。
- 长按媒体菜单能添加任务。
- 多选后能批量添加任务。
- 队列页能实时显示新任务。
- 取消任务后该任务从列表消失。
- 清空队列后列表为空。

### 阶段 4.1：缓存与多选修复

状态：已完成✅

目标：

- 修复从任务队列切回浏览本地会重新扫描的问题。
- 将扫描结果缓存到 app 目录，回到浏览本地时优先展示缓存。
- 相册总览右侧新增“选择”按钮。
- 进入选择模式后顶部切换为“压缩 / 删除 / 完成”操作区。
- 点击媒体卡片可选中/取消选中，并显示选中状态。
- 批量压缩/删除按钮会把选中媒体加入对应任务队列。

测试标准：

- 切到任务队列再切回浏览本地，不自动重新扫描。
- 重新打开浏览本地时能快速显示上次扫描结果。
- 点击选择后可以多选媒体。
- 被选中的媒体有明显选中标记。
- 批量压缩/删除会把选中项加入任务队列。

### 阶段 4.2：选择与扫描进度优化

状态：已完成✅

目标：

- 使用 Android 原生 `CheckBox` 展示媒体多选状态。
- 修复点击“选择”后预览图列表白屏刷新。
- 修复“压缩”和“删除”按钮进入选择模式时显示不同步。
- 扫描相册时显示真实可见的实时进度。

测试标准：

- 点击“选择”后下方缩略图不出现整屏白屏闪烁。
- “压缩”和“删除”按钮同时出现，按钮状态切换顺滑。
- 勾选/取消勾选单个媒体时只更新对应卡片状态。
- 重新扫描时进度条能从 0% 逐步变化到 100%。
- 扫描完成后仍能缓存结果，并正常筛选、排序、预览和批量入队。

### 阶段 4.3：选择交互性能修复

状态：已完成✅

目标：

- 点击“选择”后，压缩和删除按钮必须立刻出现。
- 点击图库项目后，复选框选中状态必须快速响应。
- 复选框放到缩略图左上角，并保留 Android 原生勾选动画。
- 避免选择模式切换时对全部媒体项发送刷新通知。

测试标准：

- 点击“选择”后顶部按钮无明显延迟。
- 点击单个媒体后，左上角复选框能立即变更选中状态。
- 大量媒体列表下选择模式仍保持流畅。
- 缩略图不重新加载、不白屏闪烁。
- 批量压缩/删除入队功能不受影响。

### 阶段 4.4：移除卡片复选框

状态：已完成✅

目标：

- 删除媒体卡片上的复选框组件和相关资源。
- 保留顶部“选择 / 压缩 / 删除 / 完成”按钮。
- 保留点击媒体进入选中集合的逻辑。
- 继续用卡片透明度变化表示是否被选中。

测试标准：

- 进入选择模式后，媒体卡片上不再显示复选框。
- 顶部批处理按钮仍可正常使用。
- 点击媒体后仍可加入压缩或删除队列。
- 缩略图区域更干净，没有残留占位或错位。

### 阶段 5：图片压缩

状态：已完成✅

目标：

- 实现真实图片压缩。
- 压缩结果保存到系统相册。

主要实现：

- 使用 `ContentResolver.openInputStream(uri)` 读取原图。
- 使用 `BitmapFactory.decodeStream` 解码。
- 根据压缩档位处理：
  - 无感：最长边 2560，JPEG 质量 88
  - 平衡：最长边 1920，JPEG 质量 74
  - 强力：最长边 1280，JPEG 质量 58
- 如果原图长边超过目标长边，按比例缩放。
- 输出 JPEG 到 app cache。
- 再通过 `MediaStore.Images.Media` 写入系统相册。
- 保存路径：`DCIM/鸭压压缩`。
- 输出文件命名：`原文件名_compressed.jpg`。

测试标准：

- 单张图片压缩任务能从 `PENDING` 变为 `RUNNING`，最后变为 `DONE`。
- 压缩过程中进度条有变化。
- 压缩后图片能在系统相册中找到。
- 输出文件大小小于或等于原图。
- 压缩失败时任务状态变为 `FAILED`，并显示失败原因。
- 失败任务可以重试。

### 阶段 5.1：切页卡顿修复

状态：已完成✅

目标：

- 分析任务队列切回浏览本地时的卡顿链路。
- 复用浏览本地和任务队列两个主 Fragment，避免每次切换重建页面。
- 给扫描结果增加内存缓存，减少反复读取 app 私有目录 JSON。
- 保留重新扫描按钮的手动刷新能力。

测试标准：

- 从任务队列切回浏览本地时不再明显卡顿。
- 切回浏览本地不会重新读取磁盘缓存和重建整页网格。
- 重新扫描仍能刷新内存缓存和磁盘缓存。
- 浏览本地、任务队列、压缩任务功能都能正常使用。

### 阶段 5.2：压缩对照相册

状态：已完成✅

目标：

- 参考 Kotlin 版逻辑，把压缩前原图副本和压缩后结果图保存到同一个系统相册。
- 系统相册文件夹名称固定为 `压缩对照`。
- 用户可以直接在系统相册中查看并对比压缩效果。

主要实现：

- 使用 `MediaStore.Images.Media` 创建系统相册图片条目。
- Android 10 及以上通过 `RELATIVE_PATH` 写入 `DCIM/压缩对照`。
- Android 10 及以上写入时使用 `IS_PENDING`，避免半写入图片提前出现在图库中。
- 原图副本命名为 `原文件名_original.原扩展名`。
- 压缩结果命名为 `原文件名_compressed.jpg`。
- 原图副本尽量保留原始图片 MIME 类型，压缩结果使用 `image/jpeg`。

测试标准：

- 压缩图片任务完成后，系统相册中能看到 `压缩对照` 文件夹。
- 同一张任务图片会生成一张 `_original` 原图副本和一张 `_compressed` 压缩结果。
- `_compressed` 图片能正常打开，并且任务列表仍显示压缩后的体积。
- 原图复制或压缩结果写入失败时，任务进入失败状态，不留下半写入的图库条目。
- 连续压缩多张图片时，每张图片都能生成对应的对照文件。

### 阶段 5.3：图标与黄色主题

状态：已完成✅

目标：

- 使用指定图片替换 app 启动图标。
- 将应用主题主色调整为黄色，让界面更贴近新品牌图标。

主要实现：

- 生成 `mdpi` 到 `xxxhdpi` 的启动图标 PNG 资源。
- 为 adaptive icon 生成独立前景图，并配置黄色背景层。
- 移除 Android Studio 默认 `webp` 图标，避免同名资源冲突。
- 在 Material 主题中配置 `colorPrimary`、`colorOnPrimary`、`colorPrimaryContainer` 等黄色主题色。

测试标准：

- App 安装后桌面图标显示为新的黄色小鸭图标。
- Android 8.0 及以上桌面能正常显示 adaptive icon。
- App 内按钮、进度条等 Material 控件主色变为黄色。
- Debug 包能正常构建安装。

### 阶段 5.4：对照文件名修复

状态：已完成✅

目标：

- 修复多张图片压缩后，对照相册里原始版本和压缩版本名称不容易一一对应的问题。
- 保证同一张图片生成的两张对照图使用完全相同的批次前缀。

主要实现：

- 每个压缩任务在写入系统相册前生成一次对照批次名。
- 批次名包含原文件基名、当前时间和原图短标识，减少同名图片互相混淆。
- 原图副本命名为 `批次名_原始版本.原扩展名`。
- 压缩结果命名为 `批次名_压缩版本.jpg`。
- 原始版本和压缩版本只在最后的版本标识上不同，方便人工核对。

测试标准：

- 连续压缩 A、B、C 三张图片后，每张图片都有一组同前缀的 `原始版本` 和 `压缩版本`。
- 同名图片连续压缩时，不同任务之间的批次前缀不同。
- 同一任务中的两张对照图不会和其他任务的对照图混淆。
- 压缩任务仍能正常完成，任务列表仍显示压缩后的实际体积。

### 阶段 5.5：压缩图 EXIF 保留

状态：已完成✅

目标：

- 修复压缩结果图丢失 ISO、焦距、拍摄设备等 EXIF 信息的问题。
- 在重新编码 JPEG 后，把原图中的拍摄元数据写回压缩结果。

主要实现：

- 使用系统 `android.media.ExifInterface` 读取原图 EXIF。
- 压缩图写入临时 JPEG 后，再写回常见拍摄标签。
- 保留设备厂商、设备型号、拍摄时间、ISO、焦距、光圈、曝光、闪光灯、白平衡、GPS、方向等信息。
- 不复制压缩后已经变化的图片宽高、缩略图偏移等结构性标签，避免元数据失真。
- 原图没有 EXIF 或格式不支持 EXIF 时，压缩任务仍正常完成。

测试标准：

- 使用带 EXIF 的照片压缩后，压缩版本仍能查看到拍摄设备信息。
- 压缩版本保留 ISO 和焦距信息。
- 压缩版本保留拍摄时间和方向信息。
- 原图没有 EXIF 时，压缩流程不崩溃。
- `assembleDebug` 构建通过。

### 阶段 5.6：照片压缩方案优化

状态：已完成✅

目标：

- 修复“视觉无损”对超清照片压缩过重的问题。
- 将原先接近 `2560 + JPEG 88` 的轻量压缩策略调整为“朋友圈画质”。
- 为不同照片提供更灵活的视觉无损压缩策略。

主要实现：

- `视觉无损` 改为自适应策略，不再默认把长边压到 2560。
- 3000 万像素以内、长边不超过 7000 的照片默认保持原始分辨率。
- 超大图或巨型图才按 7000 长边做安全缩放。
- 根据图片细节复杂度动态选择 JPEG 质量：
  - 高细节图：96
  - 普通照片：94
  - 低细节图：92
- `朋友圈画质` 使用长边 2560、JPEG 质量 88，适合聊天和社交平台分享。
- 调整队列预估体积逻辑，让两个档位的预估更接近实际定位。

测试标准：

- 对 `4284 x 5712` 一类超清照片使用视觉无损时，输出仍保持原始分辨率。
- 放大检查墙面纹理、雕塑边缘、文字或复杂纹理时，不出现明显糊化。
- 使用朋友圈画质时，输出长边约为 2560，文件体积明显下降。
- 两个档位的设置弹窗文案清楚区分“视觉无损”和“朋友圈画质”。
- 压缩结果继续保留 EXIF 信息。
- `assembleDebug` 构建通过。

### 阶段 5.7：队列进度刷新防闪

状态：已完成✅

目标：

- 修复压缩进度条更新时，左侧缩略图跟着跳动或闪动的问题。
- 让队列卡片只更新发生变化的文字、按钮和进度条。

主要实现：

- 队列适配器不再在每次进度变化时直接 `notifyDataSetChanged()`。
- 为任务列表保存轻量快照，对比状态、进度、输出体积、失败原因和档位变化。
- 列表结构不变时使用 `notifyItemChanged(position, payload)` 做局部刷新。
- 局部刷新只更新状态、输出体积、说明文字、进度条和按钮，不重新加载缩略图。
- 关闭队列 `RecyclerView` 的 change animation，避免频繁进度更新触发卡片淡入淡出。

测试标准：

- 压缩任务运行时，进度条能正常连续更新。
- 左侧缩略图不再随着进度更新闪动或重新跳动。
- 状态从待处理、处理中、已完成、失败切换时 UI 仍正确。
- 取消、重试、优先、设置按钮状态仍正确。
- 新增、删除、清空或调整任务顺序时列表仍能刷新。
- `assembleDebug` 构建通过。

### 阶段 5.8：任务队列完成态与回收重构

状态：已完成✅

参考：

- iOS 参考代码路径：`/Users/borry5/Documents/鸭压/IOS-/HelloWorldDemo/Features/Dashboard/QueueView.swift`
- 重点参考点：
  - `completedTasksButton`
  - `CompletedQueueSheet`
  - `savedText`
  - `completedVideoActionBar`
  - `deleteOriginalAssetForCompletedCompression`
  - `deleteCompressedAssetForCompletedCompression`
  - `recompressCompletedVideo`
  - `clearCompletedCompressionQueueItems`

总目标：

- 重构任务队列页面，让顶部核心操作固定，列表滚动时不丢失主操作。
- 完成任务不再混在主队列中干扰当前任务，而是进入“已完成任务”入口。
- 完成任务文案从“预计省”改为“已节省”。
- 增加安全的一键回收能力，必须使用 Android 系统相册删除/回收授权接口。
- 为完成任务提供回收原媒体、回收压缩媒体、重新压缩等后续操作。

Android 官方规范约束：

- Android 11 及以上批量回收媒体必须使用 `MediaStore.createTrashRequest()`，系统会弹出授权框。
- 如果需要永久删除才使用 `MediaStore.createDeleteRequest()`；本阶段优先做“回收/移入废纸篓”。
- 不允许直接用 `File.delete()` 删除系统相册里的原始媒体。
- Android 10 附近机型如遇单个媒体写入/删除权限问题，需要处理 `RecoverableSecurityException` 或走系统授权。

#### 阶段 5.8.0：任务拆分与验收计划

状态：已完成✅

目标：

- 将 5.8 拆成可逐步验收的小阶段。
- 明确每个子阶段的功能范围、测试标准和实现顺序。

测试标准：

- 计划文档包含 5.8.0 到 5.8.6。
- 每个子阶段都有清楚目标和验收标准。
- 明确系统相册回收必须走 `MediaStore` 授权接口。
- 用户确认后再进入 5.8.1 代码实现。

#### 阶段 5.8.1：队列页文案与吸顶布局

状态：已完成✅

目标：

- 调整任务队列顶部结构，让关键操作固定。
- 去掉当前说明文案：“当前阶段已接入图片压缩，开始队列后会顺序处理图片任务。”
- 去掉当前“待处理”统计卡片，减少顶部占用。
- 列表向上滑动时保留标题和关键操作。

主要实现：

- 顶栏固定展示：
  - `任务队列`
  - 清空按钮
  - 开始/暂停按钮
- 保留空间统计卡片：
  - `已节省空间`
  - `预计剩余`
- “已完成任务”入口作为固定区域中的独立按钮或行。
- 主队列列表只展示待处理、处理中、失败或需要用户处理的任务。

测试标准：

- 向上滚动任务列表时，`任务队列 / 清空 / 开始` 始终可见。
- 页面不再显示阶段说明文案。
- 页面不再显示旧的“待处理 X 项”大卡片。
- 当前无任务时显示空状态，不遮挡底部导航。
- 开始、暂停、清空按钮仍能正常工作。

#### 阶段 5.8.2：已完成任务入口与列表

状态：已完成✅

目标：

- 为完成任务生成“已完成任务”入口。
- 点击入口后显示所有已完成任务。
- 主任务队列和完成任务列表分离。

主要实现：

- `QueueManager` 增加完成任务筛选能力。
- `QueueFragment` 增加“已完成任务”入口。
- 点击入口打开独立页面、弹窗或 bottom sheet 展示完成任务列表。
- 入口显示完成任务数量。
- 完成任务列表支持空状态。

测试标准：

- 压缩完成后，主队列可不再展示该完成任务，或按设计迁移到完成入口。
- “已完成任务”入口数量准确。
- 点击入口能看到所有已完成任务。
- 没有完成任务时入口隐藏或显示空态，不能崩溃。
- 返回任务队列后，主队列状态保持正确。

#### 阶段 5.8.3：完成任务卡片与底部动作

状态：已完成✅

目标：

- 实现完成任务列表的卡片排版。
- 已完成任务文案改为“已节省 X”。
- 底部按钮按任务类型展示。

主要实现：

- 完成任务卡片展示：
  - 缩略图
  - 状态：`已完成`
  - 原始体积 `->` 压缩后体积
  - `已节省 X`
  - 进度 `100%`
- 图片任务先展示适配图片的动作：
  - 回收原图
  - 回收压缩图
  - 重新压缩
- 视频任务后续沿用同一布局：
  - 回收原视频
  - 回收压缩视频
  - 重新压缩
- 如果某个媒体已被回收，对应按钮置灰并显示已回收状态。

测试标准：

- 已完成压缩任务不再显示“预计省”，而是显示“已节省”。
- 原始体积、压缩后体积、节省体积计算正确。
- 三个底部动作按钮布局稳定，不挤压、不换行错乱。
- 对图片任务，按钮文案为原图/压缩图。
- 对视频任务，按钮文案为原视频/压缩视频。

#### 阶段 5.8.3.1：隐藏空完成入口

状态：已完成✅

目标：

- 当没有任何已完成任务时，不显示“已完成的任务”入口。
- 只有存在已完成任务时，才展示入口和数量。

测试标准：

- 刚进入任务队列且没有完成任务时，看不到“已完成的任务”入口。
- 完成至少一个任务后，入口自动出现并显示数量。
- 清空已完成任务后，入口再次隐藏。
- 主队列空状态和顶部统计布局不受影响。

#### 阶段 5.8.3.2：已完成任务二级页面

状态：已完成✅

目标：

- 点击“已完成的任务”后进入完整二级页面，而不是弹窗。
- 二级页面左上角提供返回按钮。
- “已完成的任务”入口右侧显示小箭头，暗示可以点击。
- 优化已完成任务页面排版，避免按钮和文案被弹窗宽度挤压。

主要实现：

- 任务队列页面拆成主页面和已完成页面两个容器。
- 点击已完成入口后隐藏主页面，显示已完成任务页面。
- 已完成任务页面左上角返回主任务队列。
- 已完成任务列表复用完成态卡片和底部动作按钮。
- 没有完成任务时，主页面不显示已完成入口。

测试标准：

- 有已完成任务时，主页面显示“已完成的任务”入口和右侧箭头。
- 点击入口进入完整二级页面。
- 二级页面左上角返回按钮能回到任务队列主页面。
- 已完成卡片内“回收原图 / 回收压缩图 / 重新压缩”等文案完整可读。
- 没有已完成任务时，主页面不显示入口。

#### 阶段 5.8.3.3：完成页排版与多选吸顶修复

状态：已完成✅

目标：

- 修复已完成任务二级页面标题文字显示不清晰、不完整的问题。
- 修复左上角返回按钮显示异常的问题。
- 浏览本地进入多选后，“压缩 / 删除 / 完成”固定在页面上方，不随媒体列表滚动离开屏幕。

主要实现：

- 已完成任务页左上角返回改为原生图标按钮，避免单字符按钮在不同字体设置下显示异常。
- 已完成任务页标题和清空按钮重新约束，避免互相挤压。
- 完成任务卡片底部动作按钮收紧文字尺寸，减少系统字体放大导致的截断。
- 扫描页新增固定多选操作栏，旧的 RecyclerView header 多选栏不再负责显示。

测试标准：

- 进入已完成任务二级页面后，标题“已完成的任务”清晰完整。
- 左上角返回按钮显示为正常返回箭头，点击能回到任务队列主页面。
- 完成任务卡片底部按钮至少能完整显示“回收原图 / 回收压缩图 / 重新压缩”的主要文字。
- 浏览本地点击“选择”后，压缩、删除、完成按钮固定在页面顶部。
- 媒体列表向下滚动时，多选操作按钮仍保留在页面上方。

#### 阶段 5.8.3.4：完成卡片按钮整行铺满

状态：已完成✅

目标：

- 修复已完成任务卡片底部三个按钮仍然显示不全的问题。
- 三个按钮从左到右占据整张任务卡片宽度，包括左侧预览图下方空间。
- 按钮文字必须完整显示，不再出现省略号。

主要实现：

- 将已完成任务操作按钮行从右侧信息列移到卡片底部整行。
- 卡片主体保持“左缩略图 + 右任务信息”的结构。
- 底部三个按钮独立占满整张卡片，三个按钮等宽分布。
- 保留未完成任务的取消、重试、优先等原有按钮位置。

测试标准：

- 已完成任务卡片中“回收原图 / 回收压缩图 / 重新压缩”完整显示。
- 三个按钮从卡片左侧一直铺到右侧，不再只占右侧信息栏。
- 缩略图、任务状态、体积、进度条布局不被破坏。
- 非已完成任务的取消、重试、优先按钮仍按原逻辑显示。

#### 阶段 5.8.4：系统相册回收接口

状态：已完成✅

目标：

- 实现从系统相册安全回收原始媒体或压缩结果。
- 必须弹出 Android 系统授权框。

主要实现：

- 新增媒体回收协调器，例如 `MediaTrashManager`。
- Android 11 及以上：
  - 使用 `MediaStore.createTrashRequest(contentResolver, uris, true)`。
  - 通过 `IntentSender` 启动系统授权框。
  - 根据用户确认/取消更新队列状态。
- 当前实现优先支持 Android 11 及以上系统授权回收。
- 压缩完成后保存压缩结果的系统相册 `Uri`，供“回收压缩图”使用。
- “回收原图”使用原媒体 `Uri` 发起系统回收请求。
- Android 10 及以下暂不绕过系统安全机制，显示兼容提示。

测试标准：

- 点击回收原图/原视频时弹出 Android 系统删除或移入回收站授权框。
- 点击回收压缩图/压缩视频时同样弹出系统授权框。
- 用户确认后，对应任务状态更新为已回收。
- 用户取消后，任务不应被标记为已回收。
- 不能使用 `File.delete()` 删除系统相册媒体。

#### 阶段 5.8.5：一键回收与清空完成列表

状态：已完成✅

目标：

- 实现完成任务页顶部“一键回收”。
- 实现右上角“清空列表”按钮清空已完成任务记录。

主要实现：

- 一键回收：
  - 收集所有已完成且原媒体尚未回收的任务。
  - 一次性调用系统授权接口批量回收。
  - 授权成功后逐项标记已回收。
- 清空完成列表：
  - 只清空已完成任务记录。
  - 不删除系统相册文件。
  - 入口文案显示为“清空列表”，避免和系统回收混淆。

测试标准：

- 已完成列表顶部有“一键回收”按钮。
- 点击后一批媒体触发一次系统授权框。
- 授权成功后，所有可回收原媒体标记为已回收。
- 已无可回收项时，一键回收按钮置灰或隐藏。
- 右上角垃圾桶能清空已完成任务列表。
- 清空列表不会删除任何相册媒体。

#### 阶段 5.8.6：联调验收与故障修复

状态：已完成✅

目标：

- 对 5.8 全流程做一次集成验收。
- 修复布局、状态、授权和数据持久化中的问题。

主要实现：

- 为任务队列增加 app 私有目录持久化缓存。
- 保存完成任务、压缩结果 Uri、回收状态、压缩档位和失败原因。
- 应用恢复时重新加载队列，运行中的任务恢复为待处理，避免卡在处理中。
- 重新压缩或调整档位时清理上一轮压缩结果 Uri，避免回收旧文件。

测试标准：

- 添加多张图片到队列并完成压缩。
- 主队列只展示当前未完成或失败任务。
- 完成任务入口数量准确。
- 完成任务列表能展示所有已完成任务。
- 单项回收能弹出系统授权框。
- 一键回收能弹出系统授权框。
- 重新压缩能重新加入队列或打开压缩设置。
- 清空完成列表只清任务记录，不删除媒体文件。
- 旋转屏幕、切后台再回来，完成任务状态不丢失。
- `assembleDebug` 构建通过。

#### 阶段 5.8.7：已完成任务预览与参数详情

状态：已完成✅

目标：

- 点击已完成任务卡片中的缩略图，进入完整预览界面。
- 支持查看原始文件的大图或视频预览。
- 在预览弹窗或二级页面中展示该文件的详细参数信息。

主要实现：

- 参考 iOS 版 `QueuePhotoPreviewSheet` / `QueueVideoPreviewSheet` 的结构。
- Android 侧新增“完成任务预览”弹窗或全屏 BottomSheet：
  - 图片任务：显示大图预览
  - 视频任务：显示视频播放器预览
- 预览界面展示基础信息卡：
  - 图片：文件大小、分辨率、资源类型
  - 视频：文件大小、分辨率、时长、码率
- 如果当前任务已经有压缩结果 `Uri`，则增加“原图 / 压缩后”切换入口。
- 沿用现有 `PreviewBottomSheet` 的信息展示口径，图片展示类型/分辨率/大小，视频展示时长/码率等参数。

测试标准：

- 点击已完成任务缩略图后，能正常打开预览界面。
- 图片能展示较大预览图，并能看到分辨率、文件大小等信息。
- 视频能展示预览播放器，并能看到时长、码率、分辨率等信息。
- 如果存在压缩结果，用户可以切换查看原图和压缩后资源。
- 预览关闭后，已完成任务列表状态不丢失。

#### 阶段 5.8.8：完成任务图像对比页维护

状态：已完成✅

目标：

- 将已完成任务预览从弹窗改为完整二级页面。
- 原图和压缩图同时显示，方便直接对比画面细节。
- 原图和压缩图都支持用户手动双指放大和拖动查看。
- 修复上一版“压缩后按钮看起来不能点击”的交互问题。

主要实现：

- 在任务队列页内新增图像对比二级页面。
- 点击已完成任务缩略图后进入该页面，不再弹出 BottomSheet。
- 页面上方展示原图，下方展示压缩图，中间显示对比提示和上下三角箭头。
- 使用自定义 `ZoomImageView` 支持双指缩放与拖动。
- 参数区域改为左右两列，分别展示原图和压缩图的文件大小、分辨率等信息。

测试标准：

- 点击已完成任务缩略图后进入完整图像对比页面。
- 原图在上、压缩图在下，中间有文本标签和上下三角箭头。
- 原图和压缩图都可以双指放大查看细节，并能拖动观察局部。
- 下方参数列表左右两列分别显示原图和压缩图信息。
- 点击返回按钮后回到已完成任务列表，列表状态不丢失。

#### 阶段 5.8.9：对比页快捷操作维护

状态：已完成✅

目标：

- 在图像对比二级页面直接提供已完成任务的后续处理入口。
- 减少用户在对比页和已完成任务列表之间来回切换。
- 用第三个按钮承接“重新压缩”，不再保留无效的底部关闭操作。

主要实现：

- 在对比页参数列表下方新增三个横向按钮：
  - 回收原图/原视频
  - 回收压缩图/压缩视频
  - 重新压缩
- 按钮文案和启用状态复用已完成任务卡片逻辑。
- 已回收资源对应按钮显示“已回收”并置灰。
- 点击回收按钮继续走 Android 系统相册回收授权接口。
- 点击重新压缩后任务回到待处理队列，并自动返回主任务队列页。

测试标准：

- 从已完成任务进入图像对比页后，底部能看到三个完整按钮。
- 点击回收原图/压缩图能弹出系统授权框。
- 回收成功后，对应按钮变为“已回收”且不可重复点击。
- 没有压缩结果时，“回收压缩图/视频”按钮不可点击。
- 点击重新压缩后回到主任务队列，并出现待处理任务。

#### 阶段 5.8.10：对比页提示栏节省空间维护

状态：已完成✅

目标：

- 减少图像对比页中间提示区域占用的垂直空间。
- 让原图、压缩图和三角方向提示集中在同一行展示。
- 为上下两张可缩放图片保留更多可视区域。

主要实现：

- 移除原图顶部独立标题和压缩图顶部独立标题。
- 移除原先分三行展示的上三角、提示文案和下三角。
- 在两张图片之间新增一行紧凑提示栏，展示“原图 ▲    压缩图 ▼”。

测试标准：

- 图像对比页中间提示只占一行。
- 原图、上三角、压缩图、下三角在同一行内展示。
- 页面不再显示“放大查看细节差异”。
- 上下图片之间的间距明显缩小，图片查看空间更充足。

#### 阶段 5.8.11：完成列表清空确认维护

状态：已完成✅

目标：

- 防止用户误触“清空列表”后立刻丢失已完成任务记录。
- 在执行清空前明确提示该操作只清记录，不删除系统相册媒体。

主要实现：

- 为已完成任务页右上角“清空列表”按钮增加 Material 风格确认对话框。
- 弹窗说明只会清空列表记录，不会删除原图和压缩图。
- 只有用户明确点击确认后，才调用 `clearCompletedTasks()`。

测试标准：

- 点击“清空列表”后先弹出确认对话框。
- 点击“取消”时，已完成任务列表保持不变。
- 点击“确认清空”后，已完成任务列表被清空。
- 清空后系统相册里的原图和压缩图仍然保留。

#### 阶段 5.8.12：压缩档位弹窗视觉规范维护

状态：已完成✅

目标：

- 提升压缩档位弹窗里的文字可读性，避免背景与文字对比不足。
- 让档位选择弹窗同时适配浅色模式和深色模式。
- 尽量贴近 Android Material 规范中的对话框与选项层次。

主要实现：

- 将弹窗选项改为 Material 风格的卡片式单选项。
- 文案颜色改用主题属性，例如 `colorOnSurface` 和 `colorOnSurfaceVariant`。
- 选中项通过边框高亮和单选按钮共同表达状态。
- 弹窗背景和卡片颜色交由 Material DayNight 主题自动适配。

测试标准：

- 浅色模式下，标题、说明、档位名称和副文案都清晰可读。
- 深色模式下，标题、说明、档位名称和副文案都清晰可读。
- 当前选中档位有明显高亮，不会和未选中项混淆。
- 点击整个档位卡片即可切换档位，交互符合安卓原生弹窗习惯。

#### 阶段 5.8.13：压缩档位弹窗安全区与排版维护

状态：已完成✅

目标：

- 修复压缩档位弹窗内容贴边、层级拥挤和底部操作区位置不规范的问题。
- 让标题、说明、选项和关闭动作落在更符合 Android dialog 的安全区内。

主要实现：

- 将标题从系统对话框标题区移入自定义内容区，统一控制上下左右边距。
- 为弹窗根布局增加明确的内容安全边距。
- 调整标题、说明、选项卡片和底部关闭按钮之间的垂直节奏。
- 将关闭动作放回弹窗内容底部右侧，形成清晰的操作区。

测试标准：

- 弹窗四周留白明显，不会出现内容贴边。
- 标题、说明、两个档位选项和关闭按钮之间层级清楚。
- 关闭按钮位于底部右侧，和内容区保持合适距离。
- 系统字体调大后，文案排版仍然稳定，不会挤压到边缘。

#### 阶段 5.8.14：完成任务双版本回收确认

状态：已完成✅

目标：

- 避免用户在不注意的情况下同时回收同一组任务的原图和压缩图。
- 当原图或压缩图其中一份已经被回收，再回收另一份前进行二次确认。

主要实现：

- 单项点击“回收原图/原视频”时，如果压缩版本已回收，先弹出确认对话框。
- 单项点击“回收压缩图/压缩视频”时，如果原始版本已回收，先弹出确认对话框。
- “一键回收原图”遇到存在压缩版本已回收的任务时，也先弹出确认对话框。
- 用户确认后，再继续调用 Android 系统相册回收授权接口。

测试标准：

- 原图和压缩图都未回收时，点击任一回收按钮直接进入系统授权流程。
- 原图已回收后，再点击回收压缩图，会先出现二次确认。
- 压缩图已回收后，再点击回收原图，会先出现二次确认。
- 点击取消时，不触发系统回收授权框。
- 点击继续回收时，继续弹出 Android 系统回收授权框。

#### 阶段 5.8.15：任务卡片按钮与体积排版维护

状态：已完成✅

目标：

- 移除未完成任务卡片中的“优先”按钮及其相关逻辑。
- 解决任务卡片中文字渲染不清晰、尺寸文案挤在一行的问题。
- 让“预估”标识单独落在右侧输出尺寸下方。

主要实现：

- 删除 `prioritizeTask` 相关逻辑、适配器回调和前端按钮。
- 任务卡片尺寸区域改成：
  - 左侧原始大小
  - 中间箭头
  - 右侧输出大小
  - 输出大小下方单独显示“预估”
- 调整状态文字、码率文字和元信息的字号、行高与 `includeFontPadding`。

测试标准：

- 未完成任务卡片中不再显示“优先”按钮。
- 左右尺寸保持在同一行显示。
- “预估”在右侧尺寸下方单独一行展示。
- 文案不再出现被截断、显示一半或挤压错位的情况。

#### 阶段 5.8.16：取消按钮截断修复

状态：已完成✅

目标：

- 修复任务卡片左下角“取消”按钮文字被截断的问题。
- 在保持现有按钮风格的前提下，让按钮有更稳定的文字显示空间。

主要实现：

- 提高“取消”按钮高度和最小高度。
- 增加上下内边距，避免文字贴边。
- 放宽文字渲染空间，减少当前字体在按钮内被裁掉的风险。

测试标准：

- 未完成任务卡片中的“取消”文字完整显示。
- 按钮高度与缩略图下方区域协调，不显得拥挤。
- 不影响“取消”按钮点击区域和交互。

#### 阶段 5.8.17：完成任务按钮状态与文案维护

状态：已完成✅

目标：

- 当原图已经回收后，禁止继续点击“重新压缩”。
- 让回收完成后的按钮文案更明确，区分原图和压缩图。

主要实现：

- 完成任务卡片中：
  - 原图已回收后，“重新压缩”按钮置灰不可点击。
  - 原图回收完成后按钮显示“原图已回收”。
  - 压缩图回收完成后按钮显示“压缩图已回收”。
- 对比预览页中的同名按钮同步沿用同一套状态逻辑。
- 视频任务同步使用“原视频已回收”与“压缩视频已回收”文案。

测试标准：

- 点击回收原图后，按钮显示“原图已回收”，且“重新压缩”按钮变灰。
- 点击回收压缩图后，按钮显示“压缩图已回收”。
- 对比预览页内的按钮状态与完成任务卡片保持一致。
- 视频任务在相同场景下也显示对应的原视频/压缩视频文案。

#### 阶段 5.8.18：图像对比窗口与同步缩放维护

状态：已完成✅

目标：

- 缩小图像对比页中上下两个图片预览窗口的垂直高度。
- 在尽量不牺牲细节查看能力的前提下，让用户无需滑动或少量滑动即可同时看到原图和压缩图。
- 实现上下两张图片的同步缩放与同步拖动：操作上方原图时，下方压缩图跟随相同缩放比例和位置；反向操作压缩图时，原图也同步。

执行计划：

1. 调整图像对比页布局：
   - 找到对比页中两个 `ZoomImageView` 的容器高度或权重设置。
   - 将单个预览窗口高度适当缩小，优先保证标题、标签行、上下图片和底部操作区不互相挤压。
   - 保持图片区域宽度不变，避免横向视觉面积明显变小。
2. 扩展缩放视图状态：
   - 为 `ZoomImageView` 增加可读取当前缩放比例、平移位置的状态接口。
   - 增加外部设置缩放状态的方法，用于把一个图片窗口的操作同步到另一个窗口。
   - 增加同步保护标记，避免两个视图互相触发导致循环更新。
3. 接入双向同步：
   - 原图被双指缩放或拖动时，把比例和位移同步给压缩图。
   - 压缩图被双指缩放或拖动时，把比例和位移同步给原图。
   - 初始进入页面时，两张图都使用同样的适配状态，避免一开始显示区域不一致。
4. 兼容性处理：
   - 如果原图和压缩图分辨率比例不同，按归一化中心点和缩放比例同步，避免直接复制像素位移导致错位。
   - 如果某张图还没加载完成，暂存同步状态，加载完成后再应用。

实际实现：

- 将图像对比页上下两个 `ZoomImageView` 高度从 `330dp` 调整为 `290dp`。
- `ZoomImageView` 新增缩放状态监听和外部应用缩放状态能力。
- 同步状态使用当前缩放倍数、图片中心点横向比例、图片中心点纵向比例。
- `QueueFragment` 为原图和压缩图建立双向监听，并用同步标记避免循环触发。

测试标准：

- 进入图像对比页后，不滑动或少量滑动即可更好地同时观察原图和压缩图。
- 双指放大原图时，压缩图同步放大到相近位置。
- 拖动原图查看局部细节时，压缩图同步移动到对应区域。
- 反向操作压缩图时，原图也能同步。
- 快速缩放、拖动时页面不闪烁、不跳回初始位置、不出现循环抖动。
- 原图和压缩图尺寸比例不同的情况下，同步仍保持大体对应区域。

#### 阶段 5.8.18.1：图像对比预览空白修复

状态：已完成✅

问题：

- 接入同步缩放后，图像对比页的上下预览区域可能只显示背景色，不显示原图和压缩图。
- 初步判断原因是图片 URI 在对比页仍处于 `GONE` 状态时绑定，`ZoomImageView` 过早根据未完成布局的宽高计算图片矩阵。

主要实现：

- 调整图像对比页进入顺序：先显示预览页面，再绑定原图和压缩图 URI。
- `ZoomImageView` 在图片或控件尺寸尚未准备好时，延迟到布局完成后再重置缩放矩阵。
- `setImageURI` 后增加下一帧矩阵校准，避免 URI 解码和布局时序不同步。

测试标准：

- 从已完成任务进入图像对比页后，上下两个预览区域都能正常显示图片。
- 原图和压缩图显示后，仍能双指缩放和拖动。
- 同步缩放功能不因为延迟矩阵校准而失效。

#### 阶段 5.8.18.2：原图对比来源稳定性修复

状态：已完成✅

问题：

- 有时图像对比页原图区域空白，但压缩图可以正常显示。
- 原因是压缩图读取的是 App 新生成到系统相册的结果 URI，而原图区域仍读取最初扫描到的相册 URI。
- 部分设备或相册同步场景下，原始 URI 可能因为云端占位、权限时序或系统相册状态变化而暂时不可读。

主要实现：

- 压缩完成时保存“压缩对照”相册中“原始版本”副本的 URI。
- `QueueTask` 新增 `originalAssetUri`，用于保存稳定的原始版本副本。
- 队列缓存同步持久化 `originalAssetUri`，避免重启 App 后丢失。
- 图像对比页优先展示 `originalAssetUri`，老任务没有该字段时再回退到原始相册 URI。
- 回收原图逻辑仍然使用真实原始媒体 URI，不误删“压缩对照”里的对照副本。

测试标准：

- 新压缩完成的图片进入图像对比页后，原图和压缩图都能正常显示。
- App 重启后进入已完成任务对比页，原图仍优先从“压缩对照”的原始版本副本加载。
- 老任务没有 `originalAssetUri` 时，仍能回退读取原始相册 URI。
- 回收原图按钮仍触发系统相册删除授权，并回收真实原图。

#### 阶段 5.8.18.3：老任务原图反查与预览故障提示

状态：已完成✅

问题：

- 部分老任务在上一阶段前已经生成，没有保存 `originalAssetUri`。
- 这些老任务进入图像对比页时仍可能读取原始相册 URI，导致原图区域空白。
- 图片无法读取时，界面只显示背景色，用户无法判断是加载中还是读取失败。

主要实现：

- 当任务没有 `originalAssetUri` 时，根据压缩图文件名中的批次前缀，在系统相册里反查同批次 `_原始版本.*` 文件。
- 反查成功后写回任务缓存，后续进入页面直接使用稳定 URI。
- 给原图和压缩图预览区域增加故障提示层。
- 如果图片加载后仍没有 Drawable，则显示“原图无法读取”或“压缩图无法读取”提示。

测试标准：

- 老任务进入图像对比页时，若“压缩对照”相册里存在同批次原始版本，原图可以重新显示。
- 读取失败时，预览框中出现明确故障提示，而不是空白背景。
- 压缩图读取失败时同样显示故障提示。
- 成功显示图片时，故障提示保持隐藏。

#### 阶段 5.8.18.4：图像对比大图采样预览修复

状态：已完成✅

问题：

- 日志出现 `Canvas: trying to draw too large bitmap`。
- 原图被 `ImageView.setImageURI` 完整解码后进入绘制流程，单张 bitmap 大约 130MB，超过系统绘制限制。
- 压缩图可以显示，是因为压缩图尺寸更小，没有触发该限制。

主要实现：

- 图像对比页不再直接使用 `setImageURI` 加载预览图。
- 后台线程先读取图片宽高，再按安全尺寸计算 `inSampleSize`。
- 将预览图最长边控制在安全范围内，并限制预估 bitmap 内存占用。
- 解码完成后回到主线程设置 `ZoomImageView`。
- 增加 `DuckyaPreview` 日志，输出原始尺寸、采样倍数和解码结果，便于后续排查。

测试标准：

- 超清原图进入图像对比页时不再出现 `trying to draw too large bitmap`。
- 原图和压缩图都能正常显示，且仍支持拖动和同步缩放。
- 页面进入时不明显卡顿，不出现长时间黑屏或无响应。
- Logcat 过滤 `DuckyaPreview` 可以看到预览解码尺寸和采样倍数。

### 阶段 6：删除到系统回收站

状态：已完成✅

目标：

- 删除任务不直接物理删除文件，而是移动到系统回收站。

主要实现：

- Android 11 及以上使用：
  - `MediaStore.createTrashRequest(resolver, uris, true)`
- 弹出系统授权窗口。
- 用户确认后任务变为 `DONE`。
- 用户拒绝后任务变为 `FAILED`。
- Android 10 及以下尝试通过 `ContentResolver.update` 设置 `IS_TRASHED = 1`。

测试标准：

- 删除图片任务能触发系统回收站授权。
- 删除视频任务能触发系统回收站授权。
- 用户确认后，任务显示删除完成。
- 用户拒绝后，任务显示失败原因。
- 删除后的媒体能在系统相册最近删除/回收站中找到。
- 批量删除时能一次性请求多个 Uri。

### 阶段 7：集成验收与修正

状态：未完成❌

目标：

- 跑通第一版完整流程。
- 修复 UI、权限、扫描、压缩、删除中的明显问题。

核心验收流程：

1. 打开 App。
2. 授权访问相册。
3. 扫描本地图片和视频。
4. 筛选图片。
5. 选择一张图片加入压缩队列。
6. 切换到任务队列。
7. 启动队列。
8. 图片压缩完成。
9. 到系统相册确认压缩图片存在。
10. 回到扫描页，选择一张图片或视频加入删除队列。
11. 启动队列。
12. 确认系统回收站授权。
13. 删除任务完成。

测试标准：

- 上述完整流程可以连续跑通。
- App 不出现明显卡死。
- 页面切换不丢关键状态。
- 权限被拒绝、媒体为空、压缩失败等情况都有提示。
- 主要按钮不会出现点击无响应。
- 队列统计中的任务数量和节省空间基本准确。

### 阶段 8：第二阶段视频压缩

状态：等待校验🟡

目标：

- 在图片压缩和任务队列稳定后，接入视频压缩。
- 支持用户按清晰度、分辨率、编码格式、码率和音频策略配置视频任务。
- 视频任务和图片任务共用同一套队列、完成任务、回收和压缩对照相册流程。

主要实现：

- 使用 AndroidX Media3 `Transformer` 执行视频转码。
- 使用 `MediaStore` 保存压缩后视频，并继续输出到系统相册“压缩对照”。
- 任务队列识别 `MediaKind.VIDEO` 后走视频 Worker，图片继续走图片 Worker。
- 视频压缩失败时进入 `FAILED`，并展示可读错误原因。
- 对不支持的编码器、分辨率或输入格式做降级与提示。

视频压缩选项设计：

- 压缩档位：
  - 高清保留：适合重要视频，优先保留细节，目标分辨率不超过 1080P，码率较高。
  - 均衡压缩：默认档位，适合日常保存，目标分辨率不超过 1080P，码率中等。
  - 分享画质：适合聊天/朋友圈分享，目标分辨率不超过 720P，码率较低。
- 分辨率：
  - 保持原分辨率
  - 1080P
  - 720P
  - 480P
- 编码格式：
  - 自动推荐
  - H.265/HEVC
  - H.264/AVC
- 码率：
  - 自动
  - 手动滑杆，建议范围 1 Mbps 到 20 Mbps
  - 快捷档位：低/中/高
- 音频：
  - 保留原音频
  - 降低音频码率
  - 静音压缩
- 其他：
  - 保留原始帧率
  - 允许降低帧率到 30fps
  - 失败时自动回退到 H.264

界面控件安排：

- 在视频任务卡片右上角继续使用设置按钮。
- 点击后打开“视频压缩设置”弹窗或二级设置页。
- 控件建议：
  - 档位使用单选卡片。
  - 分辨率使用分段按钮或单选列表。
  - 编码格式使用下拉菜单。
  - 码率使用滑杆 + 当前数值文本。
  - 音频策略使用单选列表。
  - “失败回退 H.264”使用开关。
- 任务卡片展示：
  - 原始大小
  - 预估输出大小
  - 分辨率变化
  - 码率变化
  - 视频时长

代码执行链路：

- 扫描阶段：
  - 保留现有视频扫描结果。
  - 补充视频码率估算字段或继续用大小/时长估算。
- 入队阶段：
  - `QueueTask` 支持视频压缩设置对象，例如 `VideoCompressionSettings`。
  - 图片任务使用 `CompressionSettings`，视频任务使用 `VideoCompressionSettings`。
- 执行阶段：
  - `QueueManager.processTask()` 根据 `MediaKind` 分发到图片或视频 Worker。
  - `VideoCompressionWorker` 创建 Media3 `Transformer`。
  - Worker 把进度回调给 `QueueManager.updateTaskProgress()`。
  - 转码输出先写入 app 临时文件或 MediaStore pending Uri。
  - 成功后保存压缩视频 Uri、输出大小、分辨率和码率。
  - 失败时清理临时文件并写入失败原因。
- 输出阶段：
  - 和图片一致，把“原始版本”和“压缩版本”都保存进“压缩对照”。
  - 文件命名继续使用同一组唯一基名，避免原始/压缩错配。
- 完成页阶段：
  - 完成任务卡片显示“已节省多少”。
  - 支持预览视频、回收原视频、回收压缩视频、重新压缩。

测试标准：

- 单个视频能成功压缩，进度实时更新。
- 压缩后视频能在系统相册“压缩对照”中找到。
- 原视频副本和压缩视频文件名一一对应。
- 压缩后文件大小通常小于原视频；特殊情况下若变大，应提示用户。
- 视频压缩失败时任务进入 `FAILED`，并能重新压缩。
- H.265 不可用时能自动尝试 H.264。
- 队列中图片和视频混合任务能按顺序执行。
- App 切后台再回来，队列状态不乱。
- 删除/回收视频时继续弹出 Android 系统授权框。

#### 阶段 8.0：视频压缩方案与接口设计

状态：已完成✅

目标：

- 明确视频压缩的产品档位、默认参数和数据模型。
- 避免 UI、Worker、队列三边同时改动导致返工。

主要实现：

- 新增 `VideoCompressionPreset`：
  - 高清保留
  - 均衡压缩
  - 分享画质
- 新增 `VideoCompressionSettings`：
  - preset
  - targetResolution
  - targetVideoCodec
  - targetBitrateMbps
  - audioMode
  - keepFrameRate
  - fallbackToH264
- 定义视频输出信息字段：
  - outputUri
  - outputBytes
  - outputWidth/outputHeight
  - outputBitrate
  - durationMs
- 明确默认值：
  - 默认档位：均衡压缩
  - 默认分辨率：1080P 上限
  - 默认编码：自动推荐
  - 默认码率：自动
  - 默认音频：保留原音频

测试标准：

- 视频设置模型能被 `QueueTask` 持久化和恢复。
- 图片任务不受视频设置字段影响。
- 默认视频任务入队后能拿到完整默认设置。

#### 阶段 8.1：Media3 依赖与基础转码验证

状态：等待校验🟡

目标：

- 接入 AndroidX Media3 Transformer，并先跑通最小视频转码链路。

主要实现：

- 添加依赖：
  - `androidx.media3:media3-transformer`
  - `androidx.media3:media3-common`
  - 必要时补充 `androidx.media3:media3-effect`
- 新建最小 `VideoCompressionWorker` 骨架。
- 使用单个本地视频 Uri 输出到临时文件。
- 验证 Transformer 的成功、失败和进度回调路径。

测试标准：

- Debug 构建通过。
- 单个短视频能在测试入口或临时代码中完成转码。
- 转码失败能返回错误，而不是卡住队列。

实际实现：

- 已接入 `androidx.media3:media3-transformer`、`media3-common`、`media3-effect`。
- 新增 `VideoCompressionWorker`，使用 `Transformer.start(EditedMediaItem, outputPath)` 输出到 App 私有视频目录。
- Worker 使用 `Transformer.Listener` 接收完成和失败回调，使用 `ProgressHolder` 定时读取进度。
- Debug 构建已通过。

#### 阶段 8.2：视频压缩设置弹窗

状态：等待校验🟡

目标：

- 为视频任务提供清晰、可操作的压缩设置界面。
- 设置界面需要适配浅色和深色模式。

主要实现：

- 视频任务点击设置按钮时打开视频设置弹窗。
- 档位使用单选卡片：
  - 高清保留
  - 均衡压缩
  - 分享画质
- 分辨率使用单选列表：
  - 原始
  - 1080P
  - 720P
  - 480P
- 编码格式使用菜单：
  - 自动
  - H.265
  - H.264
- 码率使用滑杆：
  - 自动模式时隐藏或置灰滑杆。
  - 手动模式显示 Mbps 数值。
- 音频策略使用单选项：
  - 保留
  - 降低码率
  - 静音
- H.264 回退使用开关。

测试标准：

- 视频任务能打开视频设置弹窗。
- 修改设置后任务卡片预估信息同步刷新。
- 深色/浅色模式文字对比清晰。
- 图片任务仍打开图片压缩档位弹窗。

实际实现：

- 新增 `VideoCompressionPreset`、`VideoCompressionSettings` 等模型。
- `QueueTask` 支持视频设置字段，并随任务缓存持久化和恢复。
- 视频任务点击设置按钮打开 `dialog_video_compression`。
- 弹窗支持档位、分辨率、编码格式、自动/手动码率、音频策略、帧率保留和 H.264 回退。
- 图片任务仍打开原图片压缩档位弹窗。

#### 阶段 8.3：VideoCompressionWorker

状态：等待校验🟡

目标：

- 实现真正的视频后台压缩逻辑。

主要实现：

- 使用 Media3 `Transformer` 执行转码。
- 根据设置生成目标分辨率、编码格式和码率策略。
- 支持进度回调到队列。
- 支持取消当前视频任务。
- H.265 失败且允许回退时，自动重试 H.264。
- 失败后清理临时文件或未完成的 MediaStore 记录。

测试标准：

- 视频压缩期间队列进度能持续变化。
- 压缩成功后得到有效可播放视频。
- 取消任务后不留下半成品文件。
- H.265 不可用设备能回退 H.264。

实际实现：

- `VideoCompressionWorker` 后台执行 Media3 转码，成功后返回输出文件 Uri 和体积。
- 队列处理视频任务时分发到 `VideoCompressionWorker`，图片任务继续走 `ImageCompressionWorker`。
- 分辨率选项通过 Media3 `Presentation.createForHeight(...)` 参与实际转码。
- 关闭“保留原始帧率”时，转码输出限制为 30fps。
- 静音压缩会在 `EditedMediaItem` 中移除音频。
- H.265 或自动编码失败且允许回退时，会自动重试 H.264。
- 队列停止或任务取消时，进度回调返回 false，Worker 取消 Transformer 并清理失败输出。

#### 阶段 8.4：队列混合处理接入

状态：等待校验🟡

目标：

- 图片和视频任务共用任务队列，按顺序处理。

主要实现：

- `QueueManager.processTask()` 根据媒体类型分发：
  - 图片走 `ImageCompressionWorker`
  - 视频走 `VideoCompressionWorker`
- 队列卡片支持视频专属信息：
  - 时长
  - 原码率
  - 目标码率
  - 分辨率变化
- 开始、暂停、取消、重新压缩逻辑兼容视频。

测试标准：

- 图片、视频混合队列能按顺序执行。
- 视频任务失败不影响后续任务继续执行。
- 暂停后正在运行的视频任务能安全停止或标记回待处理。

实际实现：

- `QueueManager.processTask()` 已按媒体类型分发，图片继续走 `ImageCompressionWorker`，视频走 `VideoCompressionWorker`。
- 视频任务卡片继续展示码率、档位、分辨率和预估体积信息。
- 队列暂停、取消和重新压缩会通过进度回调停止当前 `Transformer`。
- 视频任务完成后进入和图片一致的完成任务流程。

#### 阶段 8.5：视频输出与压缩对照相册

状态：等待校验🟡

目标：

- 视频压缩结果也进入系统相册“压缩对照”，便于用户对比。

主要实现：

- 使用 `MediaStore.Video.Media` 写入压缩后视频。
- 保存原视频副本和压缩视频到同一系统相册。
- 沿用图片阶段的一一对应命名规则：
  - `同一基名_原始版本.mp4`
  - `同一基名_压缩版本.mp4`
- 保存输出 Uri，供完成页预览和回收使用。

测试标准：

- 压缩后在系统相册能看到“压缩对照”文件夹。
- 原视频副本和压缩视频名称能一一对应。
- 回收压缩视频时能使用保存的输出 Uri。

实际实现：

- 视频输出使用 `MediaStore.Video.Media` 写入系统相册。
- Android 10 及以上通过 `RELATIVE_PATH = Movies/压缩对照` 和 `IS_PENDING` 完成安全写入。
- 原始版本和压缩版本使用同一组批次基名，避免多视频并发或连续压缩时文件名错配。
- 完成任务保存原视频副本 Uri 和压缩视频 Uri，供预览、回收和重新压缩使用。

#### 阶段 8.6：完成任务视频预览与回收

状态：等待校验🟡

目标：

- 完成页支持视频任务的完整后续操作。

主要实现：

- 完成任务卡片显示视频压缩结果：
  - 已节省大小
  - 原大小 -> 压缩后大小
  - 原码率 -> 压缩后码率
  - 原分辨率 -> 压缩后分辨率
- 预览页支持视频播放或视频首帧展示。
- 参数列表展示：
  - 文件大小
  - 分辨率
  - 时长
  - 码率
  - 编码格式
- 底部按钮支持：
  - 回收原视频
  - 回收压缩视频
  - 重新压缩

测试标准：

- 点击完成视频任务能进入预览/详情页。
- 原视频和压缩视频参数能清楚对比。
- 回收原视频/压缩视频都触发系统授权框。
- 重新压缩后视频任务重新进入队列。

实际实现：

- 完成任务对比页识别视频任务后切换为 `VideoView` 预览。
- 原视频和压缩视频分别绑定 `MediaController`，支持用户播放查看。
- 完成页沿用回收原媒体、回收压缩媒体和重新压缩入口。
- 压缩视频回收使用保存下来的 MediaStore Uri，继续走系统授权删除/回收流程。

#### 阶段 8.7：兼容性、性能与验收修复

状态：等待校验🟡

目标：

- 处理视频压缩中的设备差异和长任务稳定性问题。

主要实现：

- 兼容性：
  - 低版本系统编码器不可用时提示或降级。
  - H.265 不可用时回退 H.264。
  - 输入视频无音频、旋转信息异常、时长为 0 时有兜底。
- 性能：
  - 长视频压缩时 UI 不阻塞。
  - 进度更新节流，避免卡片频繁闪动。
  - App 切后台/返回前台后状态可恢复。
- 验收：
  - 选择 10 秒、1 分钟、5 分钟以上视频分别测试。
  - 测试 720P、1080P、4K 视频。
  - 测试不同编码输入，如 H.264、HEVC。

测试标准：

- 长视频压缩期间 App 不 ANR。
- 进度条能稳定更新。
- 编码失败能进入失败状态并给出原因。
- 完成后系统相册、队列、回收和重新压缩都能跑通。

实际实现：

- 修复 `Transformer is accessed on the wrong thread` 故障，所有 `Transformer.start()`、`getProgress()` 和 `cancel()` 都切到专属 `HandlerThread` 执行。
- H.265 或自动编码失败时继续按设置回退到 H.264。
- 视频转码在后台线程执行，队列页只接收节流后的进度变化，避免阻塞主界面。
- 失败时清理临时输出文件，并把可读错误写回任务失败原因。

#### 阶段 8.8：视频设置弹窗与音频策略维护

状态：等待校验🟡

目标：

- 明确照片和视频压缩结果都统一放入用户可见的“压缩对照”相册。
- 视频设置默认使用 H.265 编码，并在失败时回退 H.264。
- 音频不再提供调节入口，默认保留原始音频轨道。
- 参考 iOS 版本的视频设置思路，整理 3 档画质对分辨率、码率和帧率的影响。

主要实现：

- `VideoCompressionSettings` 默认档位改为 H.265，分享画质也不再降低音频。
- 旧任务缓存中的静音/降低音频码率设置会被矫正为保留原音频。
- `VideoCompressionWorker` 明确设置不移除音频，只处理视频画面转码。
- 视频设置弹窗移除音频区域，改为画质档位、输出规格、码率和安全选项四个分组。
- 点击“高清保留 / 均衡压缩 / 分享画质”时，会自动带出对应推荐参数。

测试标准：

- 打开视频设置弹窗时，默认编码选择 H.265。
- 弹窗中不再出现降低音频码率、静音压缩等选项。
- 压缩后视频仍保留原始音频。
- 三个画质档位切换时，分辨率、码率和帧率选项同步变化。
- 弹窗在深色和浅色模式下文字清晰、分组明确，不出现横向挤压截断。
- 照片和视频的原始版本/压缩版本都继续进入同一个“压缩对照”相册名称。

#### 阶段 8.9：视频帧率与 1GB 码率限制维护

状态：等待校验🟡

目标：

- 移除“保留原始帧率”复选框。
- 在输出规格中直接展示分辨率和帧率选项。
- 按新的三档视频策略设置分辨率、帧率和码率。
- 在码率设置中增加“总文件不超过 1 GB”，方便微信发送原视频。

主要实现：

- 新增 `VideoFrameRateOption`，支持保持原始帧率和 30 FPS。
- 高视觉无损：保持原始分辨率和帧率，码率最高 9 Mbps，低码率源视频不强行升码率。
- 均衡压缩：默认 1080P，保持原始帧率，目标码率约 3.5 Mbps。
- 分享画质：默认 720P / 30 FPS，目标码率约 2 Mbps。
- 新增 `limitToOneGb` 设置，和档位码率二选一。
- `VideoCompressionWorker` 使用 Media3 `VideoEncoderSettings#setBitrate()` 传入实际目标码率。
- 队列预估和卡片文案同步识别 1GB 限制模式。

测试标准：

- 视频设置弹窗不再显示“保留原始帧率”复选框。
- 输出规格区域能看到分辨率和帧率选项。
- 切换三档画质后，对应的分辨率、帧率、码率显示符合预期。
- 选择“总文件不超过 1 GB”后，码率滑杆变为弱化不可操作状态。
- 真实压缩时 Media3 编码器使用对应目标码率。
- 旧缓存任务可以继续恢复，不因新增字段崩溃。

#### 阶段 8.10：视频设置即时保存与预估刷新修复

状态：等待校验🟡

目标：

- 视频编码固定使用 H.265，并在失败时自动尝试回退 H.264，不再展示编码选项。
- 修复视频压缩设置中切换画质档位后退出再进入恢复旧设置的问题。
- 切换画质档位时，同步刷新预估码率、预估文件体积和预计节省体积。

主要实现：

- `VideoCompressionSettings` 在模型层固定编码为 H.265，固定开启 H.264 回退。
- 视频设置弹窗移除编码格式和失败回退选项。
- 切换画质档位、分辨率、帧率、码率模式和手动码率时，立即写回 `QueueManager`。
- `QueueTask.setVideoSettings()` 重新计算预估输出体积，队列卡片通过快照变化局部刷新。

测试标准：

- 打开视频设置弹窗时看不到 H.265/H.264 选择项。
- 选择不同画质档位后关闭弹窗，再进入仍保持刚才的设置。
- 选择不同画质档位后，任务卡片的码率、预估输出体积和预计节省体积立即刷新。
- 真正压缩时优先使用 H.265，失败时自动回退 H.264。

#### 阶段 8.11：1GB 限制条件生效维护

状态：等待校验🟡

目标：

- “总文件不超过 1 GB”只对原视频大小超过 1GB 的任务生效。
- 小于或等于 1GB 的视频不允许选择该模式。
- 旧缓存里如果小视频已经保存了该模式，也要自动回到普通档位码率。

主要实现：

- 视频设置弹窗根据当前原视频体积禁用 1GB 限制选项。
- `QueueTask` 在写入或恢复视频设置时归一化小视频设置，避免 1GB 限制误生效。
- 队列卡片和 `VideoCompressionWorker` 也同步增加原视频体积判断。

测试标准：

- 原视频小于或等于 1GB 时，1GB 限制选项置灰且不可保存。
- 原视频超过 1GB 时，可以选择 1GB 限制。
- 小视频旧缓存恢复后不会显示“总文件不超过 1 GB”。
- 压缩执行时，小视频不会走 1GB 限制码率计算。

#### 阶段 8.12：视频档位与自定义展开维护

状态：等待校验🟡

目标：

- 视频设置弹窗展示 4 个档位：视觉无损、均衡压缩、分享画质、自定义。
- 只有点击“自定义”后，才展开输出规格、码率和帧率设置。
- 分辨率增加 4K、2K；帧率增加 60、30、24。

主要实现：

- `VideoCompressionPreset` 新增 `CUSTOM`。
- `VideoResolutionOption` 新增 `P2160`、`P1440`，`VideoFrameRateOption` 新增 `FPS60`、`FPS24`。
- 视觉无损预设显示 6 Mbps；均衡压缩显示最高 1080P 60 FPS；分享画质显示 720P 30 FPS。
- `QueueFragment` 根据是否选择 `CUSTOM` 控制高级参数区显隐。
- `VideoCompressionWorker` 支持 4K/2K 的目标高度和 60/24 FPS 的输出帧率限制。

测试标准：

- 弹窗内能看到 4 个视频档位。
- 点击非自定义档位时，下方高级参数区隐藏。
- 点击自定义档位时，下方高级参数区展开。
- 自定义里可以选择 4K、2K、1080P、720P、480P。
- 自定义里可以选择原始、60 FPS、30 FPS、24 FPS。
- 构建通过，视频任务保存和恢复不崩溃。

## 3. 第一版建议交付顺序

| 顺序 | 阶段 | 完成后状态 |
| --- | --- | --- |
| 1 | 阶段 1：基础工程与主框架 | 等待校验🟡 |
| 2 | 阶段 2：相册权限与媒体扫描 | 等待校验🟡 |
| 3 | 阶段 3：媒体网格与预览 | 等待校验🟡 |
| 4 | 阶段 4：任务队列基础能力 | 等待校验🟡 |
| 5 | 阶段 5：图片压缩 | 已完成✅ |
| 6 | 阶段 6：删除到系统回收站 | 已完成✅ |
| 7 | 阶段 7：集成验收与修正 | 已完成✅ |

每完成一个阶段，都先把阶段状态改为 `等待校验🟡`，由你在 Android Studio 或真机/模拟器中验证。你回复“验收通过”后，再把该阶段改为 `已完成✅` 并进入下一阶段。

## 4. 状态维护规则

- `未完成❌`：还没开始实现。
- `工作中🔵`：当前正在实现。
- `等待校验🟡`：代码已完成，等待用户验证。
- `已完成✅`：用户验证通过。

阶段推进规则：

1. 开始某阶段前，把该阶段状态改为 `工作中🔵`。
2. 阶段代码完成后，把状态改为 `等待校验🟡`。
3. 用户验收通过后，把状态改为 `已完成✅`。
4. 用户验收不通过时，保持 `工作中🔵`，继续修正。

## 5. 第一版不做的内容

第一版暂不实现：

- 视频压缩。
- 复杂缩略图磁盘缓存。
- 压缩日志导出。
- 数据库持久化队列。
- 完整复刻 Kotlin 版所有 UI 细节。

这些功能可以在第一版验收通过后继续追加。

## 6. 推荐文件结构

```text
app/src/main/java/你的包名/
  MainActivity.java
  model/
    MediaItemInfo.java
    QueueTask.java
    CompressionSettings.java
    MediaKind.java
    QueueAction.java
    QueueStatus.java
    CompressionPreset.java
  queue/
    QueueManager.java
    QueueChangeListener.java
  ui/
    ScanFragment.java
    QueueFragment.java
    PreviewBottomSheet.java
    MediaGridAdapter.java
    QueueTaskAdapter.java
  util/
    MediaStoreScanner.java
    ThumbnailLoader.java
    ImageCompressor.java
    TrashHelper.java
    FormatUtils.java
```

```text
app/src/main/res/layout/
  activity_main.xml
  fragment_scan.xml
  fragment_queue.xml
  item_media_grid.xml
  item_queue_task.xml
  sheet_preview.xml
  dialog_compression_settings.xml
```

## 7. 最小演示脚本

最终课堂演示建议按这个顺序：

1. 打开 App，展示底部两个 Tab。
2. 在 `浏览本地` 页面申请相册权限。
3. 展示扫描出的图片和视频网格。
4. 切换筛选：全部、图片、视频。
5. 打开一张图片预览。
6. 点击压缩，加入队列。
7. 切换到 `任务队列` 页面。
8. 点击开始，等待压缩完成。
9. 展示压缩后的文件大小变化。
10. 回到相册或系统图库，展示 `DCIM/鸭压压缩` 中的输出图片。
11. 添加一个删除任务。
12. 展示系统回收站授权弹窗。
13. 确认删除任务完成。
