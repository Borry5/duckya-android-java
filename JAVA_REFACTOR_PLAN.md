# Java 版“鸭压”重构实施计划

> 状态图例：已完成✅ / 工作中🔵 / 等待校验🟡 / 未完成❌

## 0. 当前状态总览

| 阶段 | 状态 | 目标 |
| --- | --- | --- |
| 阶段 0：计划文档 | 已完成✅ | 明确 Java 版重构路线、阶段划分和测试标准 |
| 阶段 1：基础工程与主框架 | 已完成✅ | 搭好 Java + XML 的主界面框架 |
| 阶段 2：相册权限与媒体扫描 | 已完成✅ | 读取本地图片和视频，并展示扫描结果 |
| 阶段 3：媒体网格与预览 | 已完成✅ | 展示缩略图、媒体信息和预览弹窗 |
| 阶段 3.1：界面优化 | 已完成✅ | 优化相册页进度卡、筛选、排序和媒体卡片展示 |
| 阶段 3.2：滚动体验优化 | 已完成✅ | 相册页顶部进度、筛选和排序随内容滚出屏幕 |
| 阶段 3.2.2：扫描 ANR 修复 | 已完成✅ | 修复扫描后 0% 卡死和未响应问题 |
| 阶段 4：任务队列基础能力 | 已完成✅ | 支持添加、展示、取消、清空任务 |
| 阶段 4.1：缓存与多选修复 | 已完成✅ | 缓存扫描结果，并支持相册多选批量加入队列 |
| 阶段 4.2：选择与扫描进度优化 | 已完成✅ | 优化选择模式刷新、按钮同步和扫描实时进度 |
| 阶段 4.3：选择交互性能修复 | 已完成✅ | 修复选择按钮、复选框反馈和原生控件位置体验 |
| 阶段 4.4：移除卡片复选框 | 已完成✅ | 删除下方卡片复选框，保留顶部选择和批处理按钮 |
| 阶段 5：图片压缩 | 等待校验🟡 | 完成图片压缩并保存到系统相册 |
| 阶段 5.1：切页卡顿修复 | 等待校验🟡 | 复用主页面并缓存扫描结果，修复从队列返回相册卡顿 |
| 阶段 5.2：压缩对照相册 | 等待校验🟡 | 把原图副本和压缩结果保存到系统相册“压缩对照” |
| 阶段 5.3：图标与黄色主题 | 等待校验🟡 | 更换 app 启动图标，并把主题主色改为黄色 |
| 阶段 5.4：对照文件名修复 | 等待校验🟡 | 修复多张图片压缩后原始版本和压缩版本名称错配问题 |
| 阶段 5.5：压缩图 EXIF 保留 | 等待校验🟡 | 压缩后保留 ISO、焦距、拍摄设备等照片元数据 |
| 阶段 6：删除到系统回收站 | 未完成❌ | 调用系统回收站授权并完成删除任务 |
| 阶段 7：集成验收与修正 | 未完成❌ | 跑通核心流程并修复问题 |
| 阶段 8：第二阶段视频压缩 | 未完成❌ | 接入 Media3 Transformer 做视频压缩 |

## 1. 项目目标

Java 版第一版目标是做出一个核心可运行的 Android 相册清理压缩工具：

- 使用 Java + XML，不使用 Compose。
- 支持扫描本地图片和视频。
- 支持图片/视频预览。
- 支持把图片加入压缩队列。
- 支持把图片或视频加入删除队列。
- 支持图片压缩并保存到系统相册。
- 支持 Android 11+ 系统回收站授权删除。
- 视频压缩放到第二阶段实现。

第一版不追求完整复刻 Kotlin 参考代码的所有高级能力，优先保证流程清楚、代码稳定、容易演示。

## 2. 阶段拆分与验收标准

### 阶段 1：基础工程与主框架

状态：已完成✅

目标：

- 在 Android Studio 中创建 Java 基础项目。
- 使用 XML 布局搭建主界面。
- 用底部导航切换两个页面：`浏览本地`、`任务队列`。

主要实现：

- `MainActivity`
- `ScanFragment`
- `QueueFragment`
- `activity_main.xml`
- 底部 `BottomNavigationView`
- Fragment 容器

测试标准：

- App 可以正常启动。
- 底部有两个 Tab。
- 点击 `浏览本地` 和 `任务队列` 可以正常切换页面。
- 横竖屏切换时不崩溃。
- Logcat 无明显启动异常。

### 阶段 2：相册权限与媒体扫描

状态：已完成✅

目标：

- 申请相册读取权限。
- 通过 `MediaStore` 扫描系统相册里的图片和视频。

主要实现：

- Android 13 及以上申请：
  - `READ_MEDIA_IMAGES`
  - `READ_MEDIA_VIDEO`
- Android 12 及以下申请：
  - `READ_EXTERNAL_STORAGE`
- 图片扫描使用 `MediaStore.Images.Media`。
- 视频扫描使用 `MediaStore.Video.Media`。
- 扫描字段包括：
  - 文件名
  - Uri
  - 文件大小
  - 修改时间
  - 宽高
  - 视频时长

测试标准：

- 首次打开页面时，未授权状态能显示授权按钮。
- 点击授权按钮后能弹出系统权限申请。
- 用户授权后能扫描出本机图片和视频。
- 用户拒绝权限后页面不崩溃，并能继续提示授权。
- 没有媒体文件时显示空状态。

### 阶段 3：媒体网格与预览

状态：已完成✅

目标：

- 用三列网格展示扫描结果。
- 支持筛选、排序和媒体预览。

主要实现：

- `RecyclerView` + `GridLayoutManager`
- `MediaGridAdapter`
- 媒体卡片展示：
  - 缩略图
  - 文件大小
  - 图片 MP 信息或视频时长
- 筛选：
  - 全部
  - 图片
  - 视频
- 排序：
  - 按体积
  - 按时间
- `PreviewBottomSheet`
  - 展示大图或视频首帧
  - 展示名称、类型、大小、分辨率、时长、码率
  - 提供压缩和删除入口

测试标准：

- 图片和视频能在网格中显示。
- 图片缩略图能正常加载。
- 视频至少能显示首帧或视频占位图。
- 点击媒体能打开预览弹窗。
- 预览弹窗信息和原文件基本一致。
- 筛选全部/图片/视频结果正确。
- 按体积和按时间排序结果正确。

### 阶段 3.1：界面优化

状态：已完成✅

目标：

- 扫描完成后只展示 100% 进度条和重新扫描按钮，不再展示图片/视频分别数量。
- 新增独立筛选按钮和排序按钮，样式接近参考图但使用 Android 原生/Material 支持控件。
- 筛选中的资源类型功能真实生效。
- 默认按体积从大到小排序。
- 媒体缩略图卡片不显示文件名。

测试标准：

- 扫描完成后进度显示为 100%，按钮为重新扫描。
- 筛选按钮能打开筛选面板，全部/图片/视频切换后网格正确变化。
- 排序按钮能打开排序菜单，默认是按体积从大到小。
- 媒体卡片只显示缩略图、时长或 MP、文件大小，不显示文件名。
- 点击媒体仍能打开预览弹窗。

### 阶段 3.2：滚动体验优化

状态：已完成✅

目标：

- 相册页的标题、读取进度、筛选按钮、排序按钮和提示文案都作为内容参与滚动。
- 用户向下滑动后，顶部控制区会滑出屏幕，只剩媒体预览网格。

测试标准：

- 在媒体网格上向上滑动时，读取进度卡、筛选和排序栏会一起向上消失。
- 滚动过程中底部导航保持可用。
- 筛选、排序和预览功能仍正常。

### 阶段 3.2.2：扫描 ANR 修复

状态：已完成✅

目标：

- 修复授权后开始扫描时进度停在 0%，稍后应用未响应的问题。
- 保留 3.2 的滚动体验：顶部进度卡、筛选、排序可随内容滑出屏幕。

修复策略：

- 禁止把大量媒体项放进 `NestedScrollView` 中一次性测量。
- 改为单个 `RecyclerView` 承载 Header 和网格媒体项，Header 占满 3 列，媒体项继续按 3 列虚拟化加载。

测试标准：

- 授权后扫描不再卡死或弹出未响应。
- 扫描完成后进度变为 100%。
- 大量媒体时仍能滚动浏览。
- 顶部 Header 仍能随媒体列表一起滑出屏幕。

### 阶段 4：任务队列基础能力

状态：已完成✅

目标：

- 建立任务队列数据层。
- 支持从扫描页添加压缩或删除任务。
- 队列页能展示任务列表和基础操作。

主要实现：

- `QueueManager` 单例
- `QueueTask`
- `MediaItemInfo`
- `CompressionSettings`
- 枚举：
  - `MediaKind`
  - `QueueAction`
  - `QueueStatus`
  - `CompressionPreset`
- 队列能力：
  - 添加单个任务
  - 批量添加任务
  - 取消任务
  - 清空队列
  - 重试失败任务
  - 优先处理任务
- UI 能显示：
  - 任务名称
  - 操作类型
  - 状态
  - 原始大小
  - 预计节省空间
  - 进度条

测试标准：

- 从预览弹窗点击压缩后，图片能加入压缩队列。
- 从预览弹窗点击删除后，图片或视频能加入删除队列。
- 长按媒体菜单能添加任务。
- 多选后能批量添加任务。
- 队列页能实时显示新任务。
- 取消任务后该任务从列表消失。
- 清空队列后列表为空。

### 阶段 4.1：缓存与多选修复

状态：已完成✅

目标：

- 修复从任务队列切回浏览本地会重新扫描的问题。
- 将扫描结果缓存到 app 目录，回到浏览本地时优先展示缓存。
- 相册总览右侧新增“选择”按钮。
- 进入选择模式后顶部切换为“压缩 / 删除 / 完成”操作区。
- 点击媒体卡片可选中/取消选中，并显示选中状态。
- 批量压缩/删除按钮会把选中媒体加入对应任务队列。

测试标准：

- 切到任务队列再切回浏览本地，不自动重新扫描。
- 重新打开浏览本地时能快速显示上次扫描结果。
- 点击选择后可以多选媒体。
- 被选中的媒体有明显选中标记。
- 批量压缩/删除会把选中项加入任务队列。

### 阶段 4.2：选择与扫描进度优化

状态：已完成✅

目标：

- 使用 Android 原生 `CheckBox` 展示媒体多选状态。
- 修复点击“选择”后预览图列表白屏刷新。
- 修复“压缩”和“删除”按钮进入选择模式时显示不同步。
- 扫描相册时显示真实可见的实时进度。

测试标准：

- 点击“选择”后下方缩略图不出现整屏白屏闪烁。
- “压缩”和“删除”按钮同时出现，按钮状态切换顺滑。
- 勾选/取消勾选单个媒体时只更新对应卡片状态。
- 重新扫描时进度条能从 0% 逐步变化到 100%。
- 扫描完成后仍能缓存结果，并正常筛选、排序、预览和批量入队。

### 阶段 4.3：选择交互性能修复

状态：已完成✅

目标：

- 点击“选择”后，压缩和删除按钮必须立刻出现。
- 点击图库项目后，复选框选中状态必须快速响应。
- 复选框放到缩略图左上角，并保留 Android 原生勾选动画。
- 避免选择模式切换时对全部媒体项发送刷新通知。

测试标准：

- 点击“选择”后顶部按钮无明显延迟。
- 点击单个媒体后，左上角复选框能立即变更选中状态。
- 大量媒体列表下选择模式仍保持流畅。
- 缩略图不重新加载、不白屏闪烁。
- 批量压缩/删除入队功能不受影响。

### 阶段 4.4：移除卡片复选框

状态：已完成✅

目标：

- 删除媒体卡片上的复选框组件和相关资源。
- 保留顶部“选择 / 压缩 / 删除 / 完成”按钮。
- 保留点击媒体进入选中集合的逻辑。
- 继续用卡片透明度变化表示是否被选中。

测试标准：

- 进入选择模式后，媒体卡片上不再显示复选框。
- 顶部批处理按钮仍可正常使用。
- 点击媒体后仍可加入压缩或删除队列。
- 缩略图区域更干净，没有残留占位或错位。

### 阶段 5：图片压缩

状态：等待校验🟡

目标：

- 实现真实图片压缩。
- 压缩结果保存到系统相册。

主要实现：

- 使用 `ContentResolver.openInputStream(uri)` 读取原图。
- 使用 `BitmapFactory.decodeStream` 解码。
- 根据压缩档位处理：
  - 无感：最长边 2560，JPEG 质量 88
  - 平衡：最长边 1920，JPEG 质量 74
  - 强力：最长边 1280，JPEG 质量 58
- 如果原图长边超过目标长边，按比例缩放。
- 输出 JPEG 到 app cache。
- 再通过 `MediaStore.Images.Media` 写入系统相册。
- 保存路径：`DCIM/鸭压压缩`。
- 输出文件命名：`原文件名_compressed.jpg`。

测试标准：

- 单张图片压缩任务能从 `PENDING` 变为 `RUNNING`，最后变为 `DONE`。
- 压缩过程中进度条有变化。
- 压缩后图片能在系统相册中找到。
- 输出文件大小小于或等于原图。
- 压缩失败时任务状态变为 `FAILED`，并显示失败原因。
- 失败任务可以重试。

### 阶段 5.1：切页卡顿修复

状态：等待校验🟡

目标：

- 分析任务队列切回浏览本地时的卡顿链路。
- 复用浏览本地和任务队列两个主 Fragment，避免每次切换重建页面。
- 给扫描结果增加内存缓存，减少反复读取 app 私有目录 JSON。
- 保留重新扫描按钮的手动刷新能力。

测试标准：

- 从任务队列切回浏览本地时不再明显卡顿。
- 切回浏览本地不会重新读取磁盘缓存和重建整页网格。
- 重新扫描仍能刷新内存缓存和磁盘缓存。
- 浏览本地、任务队列、压缩任务功能都能正常使用。

### 阶段 5.2：压缩对照相册

状态：等待校验🟡

目标：

- 参考 Kotlin 版逻辑，把压缩前原图副本和压缩后结果图保存到同一个系统相册。
- 系统相册文件夹名称固定为 `压缩对照`。
- 用户可以直接在系统相册中查看并对比压缩效果。

主要实现：

- 使用 `MediaStore.Images.Media` 创建系统相册图片条目。
- Android 10 及以上通过 `RELATIVE_PATH` 写入 `DCIM/压缩对照`。
- Android 10 及以上写入时使用 `IS_PENDING`，避免半写入图片提前出现在图库中。
- 原图副本命名为 `原文件名_original.原扩展名`。
- 压缩结果命名为 `原文件名_compressed.jpg`。
- 原图副本尽量保留原始图片 MIME 类型，压缩结果使用 `image/jpeg`。

测试标准：

- 压缩图片任务完成后，系统相册中能看到 `压缩对照` 文件夹。
- 同一张任务图片会生成一张 `_original` 原图副本和一张 `_compressed` 压缩结果。
- `_compressed` 图片能正常打开，并且任务列表仍显示压缩后的体积。
- 原图复制或压缩结果写入失败时，任务进入失败状态，不留下半写入的图库条目。
- 连续压缩多张图片时，每张图片都能生成对应的对照文件。

### 阶段 5.3：图标与黄色主题

状态：等待校验🟡

目标：

- 使用指定图片替换 app 启动图标。
- 将应用主题主色调整为黄色，让界面更贴近新品牌图标。

主要实现：

- 生成 `mdpi` 到 `xxxhdpi` 的启动图标 PNG 资源。
- 为 adaptive icon 生成独立前景图，并配置黄色背景层。
- 移除 Android Studio 默认 `webp` 图标，避免同名资源冲突。
- 在 Material 主题中配置 `colorPrimary`、`colorOnPrimary`、`colorPrimaryContainer` 等黄色主题色。

测试标准：

- App 安装后桌面图标显示为新的黄色小鸭图标。
- Android 8.0 及以上桌面能正常显示 adaptive icon。
- App 内按钮、进度条等 Material 控件主色变为黄色。
- Debug 包能正常构建安装。

### 阶段 5.4：对照文件名修复

状态：等待校验🟡

目标：

- 修复多张图片压缩后，对照相册里原始版本和压缩版本名称不容易一一对应的问题。
- 保证同一张图片生成的两张对照图使用完全相同的批次前缀。

主要实现：

- 每个压缩任务在写入系统相册前生成一次对照批次名。
- 批次名包含原文件基名、当前时间和原图短标识，减少同名图片互相混淆。
- 原图副本命名为 `批次名_原始版本.原扩展名`。
- 压缩结果命名为 `批次名_压缩版本.jpg`。
- 原始版本和压缩版本只在最后的版本标识上不同，方便人工核对。

测试标准：

- 连续压缩 A、B、C 三张图片后，每张图片都有一组同前缀的 `原始版本` 和 `压缩版本`。
- 同名图片连续压缩时，不同任务之间的批次前缀不同。
- 同一任务中的两张对照图不会和其他任务的对照图混淆。
- 压缩任务仍能正常完成，任务列表仍显示压缩后的实际体积。

### 阶段 5.5：压缩图 EXIF 保留

状态：等待校验🟡

目标：

- 修复压缩结果图丢失 ISO、焦距、拍摄设备等 EXIF 信息的问题。
- 在重新编码 JPEG 后，把原图中的拍摄元数据写回压缩结果。

主要实现：

- 使用系统 `android.media.ExifInterface` 读取原图 EXIF。
- 压缩图写入临时 JPEG 后，再写回常见拍摄标签。
- 保留设备厂商、设备型号、拍摄时间、ISO、焦距、光圈、曝光、闪光灯、白平衡、GPS、方向等信息。
- 不复制压缩后已经变化的图片宽高、缩略图偏移等结构性标签，避免元数据失真。
- 原图没有 EXIF 或格式不支持 EXIF 时，压缩任务仍正常完成。

测试标准：

- 使用带 EXIF 的照片压缩后，压缩版本仍能查看到拍摄设备信息。
- 压缩版本保留 ISO 和焦距信息。
- 压缩版本保留拍摄时间和方向信息。
- 原图没有 EXIF 时，压缩流程不崩溃。
- `assembleDebug` 构建通过。

### 阶段 6：删除到系统回收站

状态：未完成❌

目标：

- 删除任务不直接物理删除文件，而是移动到系统回收站。

主要实现：

- Android 11 及以上使用：
  - `MediaStore.createTrashRequest(resolver, uris, true)`
- 弹出系统授权窗口。
- 用户确认后任务变为 `DONE`。
- 用户拒绝后任务变为 `FAILED`。
- Android 10 及以下尝试通过 `ContentResolver.update` 设置 `IS_TRASHED = 1`。

测试标准：

- 删除图片任务能触发系统回收站授权。
- 删除视频任务能触发系统回收站授权。
- 用户确认后，任务显示删除完成。
- 用户拒绝后，任务显示失败原因。
- 删除后的媒体能在系统相册最近删除/回收站中找到。
- 批量删除时能一次性请求多个 Uri。

### 阶段 7：集成验收与修正

状态：未完成❌

目标：

- 跑通第一版完整流程。
- 修复 UI、权限、扫描、压缩、删除中的明显问题。

核心验收流程：

1. 打开 App。
2. 授权访问相册。
3. 扫描本地图片和视频。
4. 筛选图片。
5. 选择一张图片加入压缩队列。
6. 切换到任务队列。
7. 启动队列。
8. 图片压缩完成。
9. 到系统相册确认压缩图片存在。
10. 回到扫描页，选择一张图片或视频加入删除队列。
11. 启动队列。
12. 确认系统回收站授权。
13. 删除任务完成。

测试标准：

- 上述完整流程可以连续跑通。
- App 不出现明显卡死。
- 页面切换不丢关键状态。
- 权限被拒绝、媒体为空、压缩失败等情况都有提示。
- 主要按钮不会出现点击无响应。
- 队列统计中的任务数量和节省空间基本准确。

### 阶段 8：第二阶段视频压缩

状态：未完成❌

目标：

- 在第一版稳定后接入视频压缩。

主要实现：

- 添加依赖：
  - `androidx.media3:media3-transformer`
  - `androidx.media3:media3-common`
- 使用 `Transformer` 执行视频转码。
- 支持参数：
  - 分辨率：原始、1080P、720P
  - 编码：H.265、H.264
  - 目标码率：默认 3 Mbps
- H.265 失败时自动回退 H.264。
- 输出到 `DCIM/鸭压压缩`。

测试标准：

- 单个视频能成功压缩。
- 压缩后视频能在系统相册找到。
- 压缩后文件大小小于或等于原视频。
- 视频压缩失败时任务进入 `FAILED`。
- H.265 不可用时能自动尝试 H.264。
- 队列中图片和视频混合任务能按顺序执行。

## 3. 第一版建议交付顺序

| 顺序 | 阶段 | 完成后状态 |
| --- | --- | --- |
| 1 | 阶段 1：基础工程与主框架 | 等待校验🟡 |
| 2 | 阶段 2：相册权限与媒体扫描 | 等待校验🟡 |
| 3 | 阶段 3：媒体网格与预览 | 等待校验🟡 |
| 4 | 阶段 4：任务队列基础能力 | 等待校验🟡 |
| 5 | 阶段 5：图片压缩 | 等待校验🟡 |
| 6 | 阶段 6：删除到系统回收站 | 等待校验🟡 |
| 7 | 阶段 7：集成验收与修正 | 已完成✅ |

每完成一个阶段，都先把阶段状态改为 `等待校验🟡`，由你在 Android Studio 或真机/模拟器中验证。你回复“验收通过”后，再把该阶段改为 `已完成✅` 并进入下一阶段。

## 4. 状态维护规则

- `未完成❌`：还没开始实现。
- `工作中🔵`：当前正在实现。
- `等待校验🟡`：代码已完成，等待用户验证。
- `已完成✅`：用户验证通过。

阶段推进规则：

1. 开始某阶段前，把该阶段状态改为 `工作中🔵`。
2. 阶段代码完成后，把状态改为 `等待校验🟡`。
3. 用户验收通过后，把状态改为 `已完成✅`。
4. 用户验收不通过时，保持 `工作中🔵`，继续修正。

## 5. 第一版不做的内容

第一版暂不实现：

- 视频压缩。
- 复杂缩略图磁盘缓存。
- 压缩日志导出。
- 数据库持久化队列。
- 完整复刻 Kotlin 版所有 UI 细节。

这些功能可以在第一版验收通过后继续追加。

## 6. 推荐文件结构

```text
app/src/main/java/你的包名/
  MainActivity.java
  model/
    MediaItemInfo.java
    QueueTask.java
    CompressionSettings.java
    MediaKind.java
    QueueAction.java
    QueueStatus.java
    CompressionPreset.java
  queue/
    QueueManager.java
    QueueChangeListener.java
  ui/
    ScanFragment.java
    QueueFragment.java
    PreviewBottomSheet.java
    MediaGridAdapter.java
    QueueTaskAdapter.java
  util/
    MediaStoreScanner.java
    ThumbnailLoader.java
    ImageCompressor.java
    TrashHelper.java
    FormatUtils.java
```

```text
app/src/main/res/layout/
  activity_main.xml
  fragment_scan.xml
  fragment_queue.xml
  item_media_grid.xml
  item_queue_task.xml
  sheet_preview.xml
  dialog_compression_settings.xml
```

## 7. 最小演示脚本

最终课堂演示建议按这个顺序：

1. 打开 App，展示底部两个 Tab。
2. 在 `浏览本地` 页面申请相册权限。
3. 展示扫描出的图片和视频网格。
4. 切换筛选：全部、图片、视频。
5. 打开一张图片预览。
6. 点击压缩，加入队列。
7. 切换到 `任务队列` 页面。
8. 点击开始，等待压缩完成。
9. 展示压缩后的文件大小变化。
10. 回到相册或系统图库，展示 `DCIM/鸭压压缩` 中的输出图片。
11. 添加一个删除任务。
12. 展示系统回收站授权弹窗。
13. 确认删除任务完成。
