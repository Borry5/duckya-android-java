# 《鸭压相册压缩工具》需求规格说明书（修订版）

## 第一章 概述

### 1.1 背景

随着手机拍照、视频录制和社交软件使用频率不断提升，用户相册中会积累大量图片和视频文件。高清照片、长视频、聊天保存文件等会持续占用本地存储空间。系统相册虽然提供了基础浏览能力，但通常缺少批量压缩、统一任务队列、压缩前后对照、完成任务管理以及安全回收等能力。

“鸭压”相册压缩工具面向 Android 本地相册管理场景，提供本地媒体扫描、浏览、多选、图片压缩、视频压缩、任务队列、已完成任务查看、系统回收站回收以及“压缩对照”相册保存等能力，帮助用户更直观地管理本机媒体空间。

### 1.2 目的

本文档用于描述当前 Android Java 重构版本“鸭压”相册压缩工具的真实需求范围、主要交互流程、功能边界、数据存储方式和非功能性要求，为课程验收、实验报告、答辩讲解和后续维护提供依据。

本文档主要读者包括：

- 项目开发成员
- 课程教师与答辩评审人员
- 测试与验收人员
- 后续维护人员

### 1.3 范围

当前版本项目范围包括：

- 申请系统相册读取权限
- 使用 MediaStore 读取本地图片和视频元数据
- 浏览本地媒体并显示三列网格
- 支持按体积、时间、视频码率排序
- 支持按全部、照片、视频筛选
- 支持点按预览和顶部多选
- 支持将媒体加入压缩队列或回收队列
- 支持图片压缩
- 支持视频压缩
- 支持任务队列开始、暂停、清空、取消、重试
- 支持已完成任务列表、完成任务预览和回收
- 支持通过系统回收站授权回收媒体
- 支持将原始版本和压缩版本保存到系统相册“压缩对照”
- 支持扫描结果缓存和任务状态持久化

当前版本不包含：

- 云端账号体系
- 服务端后台管理系统
- 多设备同步
- 远程压缩服务
- 数据库存储系统
- 用户可配置的输出容器选项
- 用户可选择的音频转码策略

### 1.4 假设与约束

- 用户设备支持 Android 媒体读取权限和 MediaStore 访问
- 删除媒体必须通过 Android 系统回收授权流程完成
- 视频压缩能力依赖设备硬件编码器与 AndroidX Media3 Transformer
- 当前项目采用 Android Java + XML 技术路线，不使用 Compose
- 当前项目不引入 Room 或 SQLite 数据库

### 1.5 业务术语

- 浏览本地：应用中用于查看本机媒体内容的一级页面
- 任务队列：统一管理压缩任务和回收任务的一级页面
- 回收：通过 Android 系统回收站授权将媒体移入系统回收站
- 压缩对照：系统相册中的固定目录，用于保存原始版本副本和压缩版本
- 已完成任务：已成功完成压缩或回收流程的任务记录

### 1.6 参考资料

- Android MediaStore 官方能力
- AndroidX Media3 Transformer
- Android RecyclerView 与 GridLayoutManager
- Material Components for Android
- 《手机软件开发项目相关要求》
- 《大作业-需求规格说明书样板》

## 第二章 系统功能概述

### 2.1 总体功能

“鸭压”当前采用底部双栏导航结构：

- 浏览本地：扫描并展示本地媒体，是添加压缩任务和回收任务的主要入口
- 任务队列：展示待处理任务、已完成任务和任务预览，是执行压缩与回收的主要入口

### 2.2 浏览本地模块

浏览本地模块主要负责相册权限申请、媒体扫描、媒体展示、排序筛选、多选和预览。

当前已实现能力包括：

- 申请相册读取权限
- 使用 MediaStore 查询图片和视频
- 显示扫描进度与已读取数量
- 显示三列方形媒体网格
- 图片卡片显示像素信息与文件大小
- 视频卡片显示时长与文件大小
- 支持按体积排序
- 支持按时间排序
- 支持按视频码率排序
- 支持筛选全部、照片、视频
- 支持顶部固定“相册总览 + 选择”栏
- 支持进入多选模式后显示固定“压缩 / 回收 / 完成”栏
- 支持批量加入压缩队列或回收队列
- 支持长按单个媒体加入压缩队列或回收队列
- 支持点按媒体进入预览弹层
- 支持扫描结果缓存，避免频繁重复扫描

### 2.3 任务队列模块

任务队列模块负责统一管理图片压缩任务、视频压缩任务和回收任务。

当前已实现能力包括：

- 显示顶部标题“任务队列”
- 支持清空当前主队列
- 支持开始和暂停队列
- 显示累计节省空间
- 显示预计剩余时间
- 显示已完成任务入口
- 展示任务卡片，包括预览图、状态、操作类型、进度、压缩前后体积与节省空间
- 支持取消任务
- 支持失败任务重试
- 支持点击设置按钮进入压缩参数弹窗
- 支持已完成任务单独列表页
- 支持已完成任务预览页
- 支持对原文件和压缩文件分别回收
- 支持在再次进入任务队列时直接回到主队列页
- 支持主队列清空确认和已完成列表清空确认

### 2.4 压缩参数设置模块

压缩参数设置模块用于设置单个压缩任务的输出策略。

当前图片压缩设置包括：

- 视觉无损
- 朋友圈画质

当前视频压缩设置包括：

- 视觉无损
- 均衡压缩
- 分享画质
- 自定义

当前视频自定义设置包括：

- 分辨率：原始 / 4K / 2K / 1080P / 720P / 480P
- 帧率：原始 / 60 FPS / 30 FPS / 24 FPS
- 码率：按档位预设或总文件不超过 1GB

当前视频压缩策略约束包括：

- 默认优先使用 H.265
- 用户界面不提供 H.264/H.265 手动切换
- H.265 失败时自动回退 H.264
- 音频始终保留原音频
- 只有原视频大于 1GB 时，才允许启用“总文件不超过 1GB”
- 只有选择“自定义”时，才展开高级设置区

### 2.5 回收模块

回收模块不直接使用文件 API 删除媒体，而是通过 Android 系统回收站授权流程处理。

当前已实现能力包括：

- Android R 及以上通过 `MediaStore.createTrashRequest` 发起系统授权
- 待回收任务可合并为一次授权请求
- 用户同意后将任务标记为完成
- 用户取消或系统异常时标记为失败
- 对已完成任务支持回收原文件或回收压缩文件
- 如果另一份文件已经回收，再回收当前文件时弹出二次确认

### 2.6 缓存与性能优化模块

当前版本的缓存与性能优化主要包括：

- 扫描结果缓存到应用私有目录 JSON 文件
- 队列任务状态持久化到应用私有目录 JSON 文件
- 浏览页使用 RecyclerView 三列网格
- 缩略图异步加载
- Android 10 及以上优先使用 `ContentResolver.loadThumbnail`
- 低版本图片采用采样解码
- 低版本视频采用 `MediaMetadataRetriever` 获取首帧
- 任务队列页面关闭高频进度刷新动画，减少闪动

## 第三章 用例分析

### 3.1 参与者

- 普通用户
- Android 系统权限与回收授权组件
- 系统相册与 MediaStore

### 3.2 用例图说明

当前系统的核心用例围绕以下主流程展开：

- 授权访问相册
- 扫描本地媒体
- 浏览与筛选本地媒体
- 预览媒体
- 多选媒体
- 添加到压缩队列
- 添加到回收队列
- 设置压缩参数
- 执行压缩任务
- 执行回收任务
- 查看任务队列与已完成任务
- 生成压缩对照相册

### 3.3 用例列表

- UC0101 授权访问相册
- UC0102 扫描本地媒体
- UC0103 浏览本地媒体
- UC0104 筛选排序媒体
- UC0105 预览媒体
- UC0106 多选媒体
- UC0201 添加到压缩队列
- UC0202 添加到回收队列
- UC0203 设置压缩参数
- UC0204 执行压缩任务
- UC0205 执行回收任务
- UC0206 查看任务队列
- UC0301 生成压缩对照相册

## 第四章 用例文档

### 4.1 UC0101 授权访问相册

基本路径：

1. 用户打开应用。
2. 系统检测相册读取权限。
3. 如果未授权，页面显示授权按钮。
4. 用户点击“授权访问相册”。
5. Android 系统弹出权限申请。
6. 用户同意授权。
7. App 进入扫描或加载缓存结果流程。

备选事件流：

- 用户拒绝授权：页面保留权限提示状态。
- Android 13 及以上请求 `READ_MEDIA_IMAGES` 与 `READ_MEDIA_VIDEO`。
- Android 12 及以下请求 `READ_EXTERNAL_STORAGE`。

### 4.2 UC0102 扫描本地媒体

基本路径：

1. 用户进入浏览本地页面。
2. 系统优先尝试读取上次扫描缓存。
3. 如果缓存不存在，用户点击开始扫描。
4. 系统通过 MediaStore 查询图片列表。
5. 系统通过 MediaStore 查询视频列表。
6. 系统合并媒体列表并按默认规则排序。
7. 页面显示扫描进度和媒体网格。

备选事件流：

- 相册为空：页面显示空结果状态。
- 读取异常：页面显示扫描失败信息。

### 4.3 UC0103 浏览本地媒体

基本路径：

1. 用户进入浏览本地页面。
2. 系统显示顶部固定标题栏。
3. 系统显示三列媒体网格。
4. 图片卡片显示缩略图、像素信息和文件大小。
5. 视频卡片显示缩略图、视频时长和文件大小。
6. 用户上下滑动浏览媒体。

备选事件流：

- 缩略图加载失败：显示占位图。
- 媒体数据缺失：使用兜底信息显示。

### 4.4 UC0104 筛选排序媒体

基本路径：

1. 用户点击筛选按钮。
2. 系统显示筛选面板。
3. 用户选择全部、照片或视频。
4. 用户点击排序按钮。
5. 系统显示排序菜单。
6. 用户选择按体积、按时间或按视频码率排序。
7. 系统刷新列表。

备选事件流：

- 筛选后无结果：页面显示 0 项。
- 排序字段缺失：使用默认值兜底。

### 4.5 UC0105 预览媒体

基本路径：

1. 用户点按媒体卡片。
2. 系统打开预览弹层。
3. 图片展示大图信息，视频展示视频信息与基础预览。
4. 用户关闭预览弹层。

备选事件流：

- 原媒体无法读取：显示失败提示。

### 4.6 UC0106 多选媒体

基本路径：

1. 用户点击顶部“选择”。
2. 顶部栏切换为“压缩 / 回收 / 完成”。
3. 用户点按多个媒体卡片。
4. 被选中媒体显示选择状态。
5. 用户点击压缩或回收。
6. 系统将所选媒体加入对应队列。
7. 用户点击完成退出多选模式。

备选事件流：

- 未选择任何媒体时，压缩和回收按钮不可执行。

### 4.7 UC0201 添加到压缩队列

基本路径：

1. 用户通过长按单个媒体或多选模式选择媒体。
2. 用户点击“压缩”。
3. 系统生成压缩任务。
4. 任务状态为待处理。
5. 用户可进入任务队列查看。

备选事件流：

- 媒体 Uri 无法访问：执行阶段可能失败。

### 4.8 UC0202 添加到回收队列

基本路径：

1. 用户通过长按单个媒体或多选模式选择媒体。
2. 用户点击“回收”。
3. 系统生成回收任务。
4. 用户进入任务队列后点击开始。
5. 系统发起系统回收授权。

备选事件流：

- 用户拒绝授权：任务失败。
- 系统接口异常：任务失败并允许重试。

### 4.9 UC0203 设置压缩参数

基本路径：

1. 用户进入任务队列。
2. 用户点击压缩任务卡片上的设置按钮。
3. 系统打开对应的压缩参数弹窗。
4. 图片任务可选择图片压缩档位。
5. 视频任务可选择视频档位，必要时展开自定义参数。
6. 系统即时保存或在应用设置后保存参数。
7. 队列任务预估信息更新。

备选事件流：

- 用户关闭弹窗：保持当前设置。
- 视频任务选择非自定义档位：高级区域收起。

### 4.10 UC0204 执行压缩任务

基本路径：

1. 用户在任务队列点击开始。
2. 系统先检查是否有待授权回收任务。
3. 若无，则按顺序取出待处理压缩任务。
4. 图片任务执行图片压缩。
5. 视频任务调用 Media3 Transformer 执行转码。
6. 系统将原始版本副本和压缩版本保存到“压缩对照”相册。
7. 系统更新任务状态为已完成。
8. 页面显示压缩前体积、压缩后体积和节省空间。

备选事件流：

- 压缩失败：任务标记失败。
- H.265 编码失败：自动回退 H.264。
- 保存失败：任务标记失败。

### 4.11 UC0205 执行回收任务

基本路径：

1. 用户在任务队列点击开始。
2. 系统收集所有待回收任务 Uri。
3. 系统调用 `MediaStore.createTrashRequest`。
4. Android 系统弹出回收确认对话框。
5. 用户同意回收。
6. 系统将任务标记为完成。

备选事件流：

- 用户拒绝授权：任务失败。
- 系统未弹出授权：任务失败并允许重试。
- 部分 Uri 无效：对应任务失败。

### 4.12 UC0206 查看任务队列

基本路径：

1. 用户点击底部“任务队列”。
2. 页面直接进入主队列页。
3. 页面显示标题、清空按钮、开始/暂停按钮。
4. 页面显示累计节省空间和预计剩余时间。
5. 页面显示任务卡片列表。
6. 用户可取消、重试、设置参数或进入已完成任务。

### 4.13 UC0301 生成压缩对照相册

基本路径：

1. 压缩任务完成。
2. 系统生成当前任务的原始版本副本和压缩版本。
3. 系统通过 MediaStore 写入系统相册“压缩对照”目录。
4. 任务记录保存原始版本和压缩版本的 Uri。

备选事件流：

- 写入失败：任务失败或缺少对应展示资源。

## 第五章 领域模型与数据模型

### 5.1 领域模型

当前系统核心对象包括：

- MediaItemInfo：本地媒体对象
- QueueTask：任务对象
- CompressionSettings：图片压缩设置
- VideoCompressionSettings：视频压缩设置
- QueueManager：任务调度中心

### 5.2 核心数据对象

主要数据对象包括：

- 图片/视频名称
- 媒体 Uri
- 文件大小
- 修改时间
- 宽高
- 视频时长
- 任务类型
- 任务状态
- 图片压缩参数
- 视频压缩参数
- 原始版本副本 Uri
- 压缩版本 Uri

### 5.3 数据存储说明

本项目主要依赖 Android 系统 MediaStore，不设计传统数据库。

当前存储方式如下：

- 原始图片和视频：存储在系统相册
- 压缩输出：通过 MediaStore 写入系统相册“压缩对照”
- 回收结果：通过系统回收站机制处理
- 扫描结果缓存：保存在应用私有目录 JSON 文件
- 任务状态：保存在应用私有目录 JSON 文件

当前版本没有引入 SQLite、Room 或远程数据库。

## 第六章 非功能性需求

### 6.1 性能需求

- 浏览本地页面应能在常见媒体数量下保持基本流畅
- 扫描过程需在后台线程执行，避免阻塞主线程
- 缩略图加载需异步执行
- 队列任务需串行执行，避免多任务同时压缩造成系统负担过高

### 6.2 安全性需求

- 媒体回收必须通过 Android 系统授权
- 不直接绕过系统权限删除用户文件
- 应用私有缓存与任务文件仅保存在应用目录中

### 6.3 可维护性需求

- 项目采用 Java + XML 分层结构
- 图片压缩、视频压缩、队列调度、扫描读取分别独立封装
- 关键功能文件应具备清晰注释和可定位代码入口

### 6.4 可扩展性需求

后续可扩展方向包括：

- 更细粒度的视频参数选项
- 数据库存储
- 更强的缩略图缓存调度
- 导出任务历史记录

### 6.5 可移植性需求

- 当前项目面向 Android 平台
- 核心媒体访问基于 Android MediaStore
- 视频压缩依赖 AndroidX Media3 Transformer 与设备编码能力

### 6.6 数据存储需求

- 任务状态应支持进程恢复后继续读取
- 扫描结果应支持缓存读取
- 媒体原始数据仍以系统相册为准

### 6.7 用户界面需求

- 底部导航结构应清晰稳定
- 浏览本地页面顶部栏固定
- 多选操作栏固定在顶部
- 任务队列页面应清晰展示统计信息和任务状态
- 已完成任务需支持独立查看和预览
- 操作文案统一使用“回收”而不是“删除”
